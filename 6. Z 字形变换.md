
**题目链接**：[https://leetcode.cn/problems/zigzag-conversion/description](https://leetcode.cn/problems/zigzag-conversion/description)

---

## 题目描述

将一个给定字符串 `s` 根据给定的行数 `numRows` ，以从上往下、从左到右进行 Z 字形排列。

比如输入字符串为 `"PAYPALISHIRING"` 行数为 `3` 时，排列如下：

```
P   A   H   N
A P L S I I G
Y   I   R
```

之后，你的输出需要从左往右逐行读取，产生出一个新的字符串，比如：`"PAHNAPLSIIGYIR"`。

**示例 1：**

**输入：**
`s = "PAYPALISHIRING", numRows = 3`
**输出：**
`"PAHNAPLSIIGYIR"`

**示例 2：**

**输入：**
`s = "PAYPALISHIRING", numRows = 4`
**输出：**
`"PINALSIGYAHRPI"`

**解释：**
```
P     I    N
A   L S  I G
Y A   H R
P     I
```

**示例 3：**

**输入：**
`s = "A", numRows = 1`
**输出：**
`"A"`

**提示：**

- `1 <= s.length <= 1000`
- `s` 由英文字母（小写和大写）、`','` 和 `'.'` 组成
- `1 <= numRows <= 1000`

## 解题思路 ：

**模拟法：** 观察 Z 字形排列的规律，字符是按行号"先往下走、再往上走、再往下走..."的顺序排列的。

以 `numRows = 3`，`s = "PAYPALISHIRING"` 为例，每个字符对应的行号是：

```
P -> 行0 ↓
A -> 行1 ↓
Y -> 行2 ↑ （到达底部，方向翻转）
P -> 行1 ↑
A -> 行0 ↓ （到达顶部，方向翻转）
L -> 行1 ↓
I -> 行2 ↑
S -> 行1 ↑
H -> 行0 ↓
I -> 行1 ↓
R -> 行2 ↑
I -> 行1 ↑
N -> 行0 ↓
G -> 行1 ↓
```

所以算法步骤是：

1. 开一个数组 `rows`，每个元素代表一行，初始为空字符串
2. 维护当前行号 `curRow = 0` 和方向标志 `goingDown = false`
3. 遍历每个字符：
   - 将字符追加到 `rows[curRow]`
   - 如果 `curRow == 0` 或 `curRow == numRows - 1`，翻转方向
   - 根据方向更新 `curRow`（+1 或 -1）
4. 最后把所有行拼接起来

**注意边界情况：** 当 `numRows == 1` 时，直接返回原字符串，因为不存在 Z 字形。

## 代码实现

## PHP 解法

```php
class Solution {

    /**
     * @param String $s
     * @param Integer $numRows
     * @return String
     */
    function convert($s, $numRows) {
        // 只有一行，直接返回原字符串
        if ($numRows == 1) {
            return $s;
        }

        // 初始化每行为空字符串
        $rows = array_fill(0, $numRows, '');
        $curRow = 0;
        $goingDown = false;

        // 遍历字符串每个字符
        for ($i = 0; $i < strlen($s); $i++) {
            // 将字符追加到当前行
            $rows[$curRow] .= $s[$i];

            // 到达顶部或底部时，翻转方向
            if ($curRow == 0 || $curRow == $numRows - 1) {
                $goingDown = !$goingDown;
            }

            // 根据方向更新行号
            $curRow += $goingDown ? 1 : -1;
        }

        // 拼接所有行
        return implode('', $rows);
    }
}
```

**PHP 语法要点：**

- `$s[$i]` 可以直接访问字符串中的单个字符，不需要 `substr()`。
- `$rows[$curRow] .= $s[$i]` 使用 `.=` 将字符追加到字符串末尾，等价于 `$rows[$curRow] = $rows[$curRow] . $s[$i]`。
- `array_fill(0, $numRows, '')` 创建一个包含 `$numRows` 个空字符串的数组，比循环初始化更简洁。
- `implode('', $rows)` 将数组所有元素拼接成一个字符串，对应 Go 的 `strings.Join()`。

## Go 解法

```go
import "strings"

func convert(s string, numRows int) string {
    // 只有一行，直接返回原字符串
    if numRows == 1 {
        return s
    }

    // 初始化每行为空字符串
    rows := make([]string, numRows)
    curRow := 0
    goingDown := false

    // 遍历字符串每个字符
    for i := 0; i < len(s); i++ {
        // 将字符追加到当前行
        rows[curRow] += string(s[i])

        // 到达顶部或底部时，翻转方向
        if curRow == 0 || curRow == numRows-1 {
            goingDown = !goingDown
        }

        // 根据方向更新行号
        if goingDown {
            curRow++
        } else {
            curRow--
        }
    }

    // 拼接所有行
    return strings.Join(rows, "")
}
```

**Go 语法要点（对比 PHP）：**

- Go 的 `string` 是不可变的，`s[i]` 返回的是 `byte` 类型（字节值），不能直接拼接到 `string` 上，需要用 `string(s[i])` 转换。PHP 中 `$s[$i]` 直接返回字符串，没有这个问题。
- Go 没有三元运算符 `?:`，所以 `$curRow += $goingDown ? 1 : -1` 在 Go 中要写成 `if/else` 分支。这是 PHP 转 Go 常见的不习惯之处。
- `make([]string, numRows)` 创建一个长度为 `numRows` 的字符串切片，所有元素默认是空字符串 `""`。PHP 中对应 `array_fill()`。
- `strings.Join(rows, "")` 将字符串切片拼接成一个字符串，对应 PHP 的 `implode()`。注意 Go 需要导入 `"strings"` 包。
- Go 的布尔取反和 PHP 一样用 `!`：`goingDown = !goingDown`。

## 关键点

这道题的核心在于把 Z 字形排列转化为**行号的折返运动**。不需要真的去构造一个二维矩阵，只需要关注每个字符属于哪一行，然后按行拼接即可。

方向翻转的条件是 `curRow == 0 || curRow == numRows - 1`，即到达顶部或底部时翻转。这个条件同时覆盖了"向下走到底"和"向上走到顶"两种情况。

容易踩的坑：
- `numRows == 1` 时必须单独处理，否则方向翻转条件永远为真，行号会在 0 和 -1 之间跳动导致越界。
- Go 中 `s[i]` 是 `byte` 不是 `string`，需要 `string(s[i])` 转换才能拼接到字符串。

## 复杂度分析

- 时间复杂度 O(N) ：遍历一次字符串，每个字符处理时间为 O(1)，最后拼接也是 O(N)。
- 空间复杂度 O(N) ：`rows` 数组存储所有字符，总长度等于原字符串长度。

#leetcode #字符串 #模拟
