
**题目链接**：[https://leetcode.cn/problems/rotate-string/description](https://leetcode.cn/problems/rotate-string/description)

---

## 题目描述

给定两个字符串 `s` 和 `goal`。如果在若干次旋转操作之后，`s` 能变成 `goal`，那么返回 `true`。

`s` 的旋转操作就是将 `s` 最左边的字符移动到最右边。

例如，若 `s = 'abcde'`，在旋转一次之后结果就是 `'bcdea'`。

**示例 1：**

```
输入：s = "abcde", goal = "cdeab"
输出：true
```

**示例 2：**

```
输入：s = "abcde", goal = "abced"
输出：false
```

**提示：**

- `1 <= s.length, goal.length <= 100`
- `s` 和 `goal` 由小写英文字母组成

## 解题思路

### 核心思想：字符串拼接 + 子串检查

如果 `s` 可以通过旋转变成 `goal`，那么 `goal` 一定是 `s + s` 的子串。

**原理分析：**

以 `s = "abcde"` 为例，所有可能的旋转结果：
- 旋转 0 次：`abcde`
- 旋转 1 次：`bcdea`
- 旋转 2 次：`cdeab`
- 旋转 3 次：`deabc`
- 旋转 4 次：`eabcd`

将 `s` 与自身拼接得到 `s + s = "abcdeabcde"`，可以看到所有旋转结果都是这个拼接字符串的子串。

**算法步骤：**

1. 首先检查两个字符串的长度是否相等，不相等直接返回 `false`
2. 将字符串 `s` 与自身拼接
3. 检查 `goal` 是否是拼接字符串的子串

**时间复杂度：** O(n²)，其中 n 是字符串长度

**空间复杂度：** O(n)，用于存储拼接后的字符串

## Go 解法

```go
func rotateString(s string, goal string) bool {
    // 长度不同，不可能通过旋转得到
    if len(s) != len(goal) {
        return false
    }
    // 检查 goal 是否是 s+s 的子串
    return strings.Contains(s+s, goal)
}
```

**说明：** `strings.Contains()` 函数用于检查子串是否存在。Go 的字符串是不可变的，拼接操作会创建新字符串。

## PHP 解法

```php
class Solution {

    /**
     * @param String $s
     * @param String $goal
     * @return Boolean
     */
    function rotateString($s, $goal) {
        // 长度不同，不可能通过旋转得到
        if (strlen($s) != strlen($goal)) {
            return false;
        }
        // 检查 goal 是否是 s+s 的子串
        return strpos($s . $s, $goal) !== false;
    }
}
```

**说明：** `strpos()` 函数用于查找子串首次出现的位置，返回 `false` 表示未找到。使用 `!== false` 进行严格比较，因为子串可能出现在位置 0。PHP 中 `.` 是字符串拼接运算符。

#leetcode #字符串
