
**题目链接**：[https://leetcode.cn/problems/rotate-image/description](https://leetcode.cn/problems/rotate-image/description)

---

## 题目描述

给定一个 `n × n` 的二维矩阵 `matrix` 表示一个图像。请你将图像顺时针旋转 90 度。

你必须在 **原地** 旋转图像，这意味着你需要直接修改输入的二维矩阵。请 **不要** 使用另一个矩阵来旋转图像。

**示例 1：**

**输入：**
`matrix = [[1,2,3],[4,5,6],[7,8,9]]`
**输出：**
`[[7,4,1],[8,5,2],[9,6,3]]`

**示例 2：**

**输入：**
`matrix = [[5,1,9,11],[2,4,8,10],[13,3,6,7],[15,14,12,16]]`
**输出：**
`[[15,13,2,5],[14,3,4,1],[12,6,8,9],[16,7,10,11]]`

**提示：**

- `n == matrix.length == matrix[i].length`
- `1 <= n <= 20`
- `-1000 <= matrix[i][j] <= 1000`

## 解题思路 ：

**两步法：** 将顺时针旋转 90° 拆解为两个简单的原地操作。

**第一步：转置矩阵**（沿主对角线翻转，即行列互换）

把 `matrix[i][j]` 和 `matrix[j][i]` 互换。注意只需要遍历上三角（`j > i`），否则会交换两次等于没换。

**第二步：翻转每一行**（左右镜像）

把每行的第一个和最后一个互换，第二个和倒数第二个互换...

以 `3×3` 矩阵为例：

```
原始矩阵：
1 2 3
4 5 6
7 8 9

第一步，转置（行列互换）：
1 4 7
2 5 8
3 6 9

第二步，翻转每行：
7 4 1
8 5 2
9 6 3   ← 顺时针旋转 90° 的结果
```

**为什么这样可以？** 观察顺时针旋转 90° 的坐标变换规律：原来 `(i, j)` 位置的元素会跑到 `(j, n-1-i)` 的位置。转置把 `(i, j)` 变到 `(j, i)`，翻转每行再把 `(j, i)` 变到 `(j, n-1-i)`，两步合起来正好就是旋转 90°。

## 代码实现

## PHP 解法

```php
class Solution {

    /**
     * @param Integer[][] $matrix
     * @return NULL
     */
    function rotate(&$matrix) {
        $n = count($matrix);

        // 第一步：转置矩阵（沿主对角线翻转）
        for ($i = 0; $i < $n; $i++) {
            for ($j = $i + 1; $j < $n; $j++) {
                // 交换 matrix[i][j] 和 matrix[j][i]
                $temp = $matrix[$i][$j];
                $matrix[$i][$j] = $matrix[$j][$i];
                $matrix[$j][$i] = $temp;
            }
        }

        // 第二步：翻转每一行
        for ($i = 0; $i < $n; $i++) {
            $left = 0;
            $right = $n - 1;
            while ($left < $right) {
                $temp = $matrix[$i][$left];
                $matrix[$i][$left] = $matrix[$i][$right];
                $matrix[$i][$right] = $temp;
                $left++;
                $right--;
            }
        }
    }
}
```

**PHP 语法要点：**

- 参数 `&$matrix` 的 `&` 是引用传递，直接修改原数组而不是拷贝。这和 Go 的切片传递（本身就是引用）不同，PHP 数组默认是值传递（拷贝），必须显式用 `&` 才能原地修改。
- PHP 其实可以用 `[$matrix[$i][$left], $matrix[$i][$right]] = [$matrix[$i][$right], $matrix[$i][$left]]` 来交换两个变量，不需要临时变量 `$temp`，这是 PHP 7.1+ 的列表赋值语法。

## Go 解法

```go
func rotate(matrix [][]int) {
    n := len(matrix)

    // 第一步：转置矩阵（沿主对角线翻转）
    for i := 0; i < n; i++ {
        for j := i + 1; j < n; j++ {
            // 交换 matrix[i][j] 和 matrix[j][i]
            matrix[i][j], matrix[j][i] = matrix[j][i], matrix[i][j]
        }
    }

    // 第二步：翻转每一行
    for i := 0; i < n; i++ {
        left := 0
        right := n - 1
        for left < right {
            matrix[i][left], matrix[i][right] = matrix[i][right], matrix[i][left]
            left++
            right--
        }
    }
}
```

**Go 语法要点（对比 PHP）：**

- Go 支持多元组赋值 `a, b = b, a`，可以直接交换两个变量，不需要临时变量。PHP 也能做到（列表赋值），但 Go 的写法更自然。
- Go 的切片传参本身就是引用，函数内修改会直接影响原数据，不需要像 PHP 那样加 `&`。这是因为切片底层是一个指向数组的指针。
- `len(matrix)` 获取行数，因为 `matrix` 是 `[][]int`，`len()` 返回外层切片的长度。

## 关键点

这道题的关键在于**把旋转拆解为两个简单操作**，而不是尝试一次性把 4 个位置的元素轮转。

转置时内层循环从 `j = i + 1` 开始非常重要：如果从 `j = 0` 开始，每对元素会被交换两次（`(i,j)↔(j,i)` 然后 `(j,i)↔(i,j)`），等于没换。只遍历上三角就能保证每对只交换一次。

**扩展：** 如果题目改为逆时针旋转 90°，只需要把两步的顺序换一下——先翻转每行，再转置。

## 复杂度分析

- 时间复杂度 O(N²) ：转置和翻转各遍历一次矩阵，N² 个元素各被访问常数次。
- 空间复杂度 O(1) ：只用了几个临时变量，原地操作。

#leetcode #数组 #矩阵 #模拟
