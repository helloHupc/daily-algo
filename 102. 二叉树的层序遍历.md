
**题目链接**：[https://leetcode.cn/problems/binary-tree-level-order-traversal/description](https://leetcode.cn/problems/binary-tree-level-order-traversal/description)

---
## 题目描述

给你二叉树的根节点 `root`，返回其节点值的**层序遍历**。（即逐层地，从左到右访问所有节点）。

**示例 1：**

```
输入：root = [3,9,20,null,null,15,7]
输出：[[3],[9,20],[15,7]]
```

**示例 2：**

```
输入：root = [1]
输出：[[1]]
```

**示例 3：**

```
输入：root = []
输出：[]
```

**提示：**

- 树中节点数目在范围 `[0, 2000]` 内
- `-1000 <= Node.val <= 1000`

## 解题思路

### 核心思想：队列辅助 BFS

层序遍历（BFS）的核心是**利用队列先进先出的特性，逐层处理节点**。

关键点在于：**如何区分每一层？**

两种主流做法：

#### 方法一：用队列大小区分层（推荐）

每次处理一层时，先记录当前队列的大小 `size`，然后一次性处理 `size` 个节点。这样每次处理完，队列里刚好是下一层的所有节点。

```
初始：queue = [root]               → size=1 → 取出 3，放入 9,20
               queue = [9, 20]     → size=2 → 取出 9,20，放入 15,7
                               queue = [15, 7] → size=2 → 取出 15,7
```

#### 方法二：用两个队列（一个存当前层，一个存下一层）

逻辑直观，但多了一个队列的空间开销。

---

### 算法流程（方法一）

1. 初始化结果数组 `result = []`，队列 `queue = [root]`
2. 如果 `root == null`，直接返回 `[]`
3. 循环直到队列为空：
   - 记录当前队列大小 `size = count(queue)`——这就是当前层的节点数
   - 创建当前层的数组 `level = []`
   - 循环 `size` 次：
     - 从队列头部取出一个节点
     - 将节点值加入 `level`
     - 如果左子节点不为空，入队
     - 如果右子节点不为空，入队
   - 将 `level` 加入 `result`
4. 返回 `result`

### 复杂度分析

- **时间复杂度**：O(n) — 每个节点恰好入队出队一次
- **空间复杂度**：O(n) — 队列中最多存储一层的节点，最坏情况（完全二叉树的最底层）有约 n/2 个节点

## PHP 解法

```php
class Solution {

    /**
     * @param TreeNode $root
     * @return Integer[][]
     */
    function levelOrder($root) {
        if ($root === null) {
            return [];
        }

        $result = [];
        $queue = [$root];

        while (!empty($queue)) {
            $size = count($queue);
            $level = [];

            for ($i = 0; $i < $size; $i++) {
                // 从队列头部取出节点（array_shift 出队）
                $node = array_shift($queue);
                $level[] = $node->val;

                if ($node->left !== null) {
                    $queue[] = $node->left;
                }
                if ($node->right !== null) {
                    $queue[] = $node->right;
                }
            }

            $result[] = $level;
        }

        return $result;
    }
}
```

**说明：**
- PHP 用 `array_shift()` 从数组头部弹出元素实现队列，但需要注意 `array_shift()` 是 O(n) 的操作（会重置数组索引）
- 也可以用 `SplQueue` 类来获得更好的队列性能：`$queue = new SplQueue(); $queue->enqueue($root); $queue->dequeue();`
- 外层 `while` 控制层数，内层 `for` 控制每一层的节点遍历

## Go 解法

```go
/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */

func levelOrder(root *TreeNode) [][]int {
    if root == nil {
        return [][]int{}
    }

    result := make([][]int, 0)
    queue := []*TreeNode{root}

    for len(queue) > 0 {
        size := len(queue)
        level := make([]int, 0, size)

        for i := 0; i < size; i++ {
            // 从队列头部取出节点
            node := queue[0]
            queue = queue[1:] // 切掉头部

            level = append(level, node.Val)

            if node.Left != nil {
                queue = append(queue, node.Left)
            }
            if node.Right != nil {
                queue = append(queue, node.Right)
            }
        }

        result = append(result, level)
    }

    return result
}
```

**说明：**
- Go 用 `queue[0]` 取队头，`queue = queue[1:]` 出队，底层共享同一个数组，没有 PHP 那种 O(n) 的开销
- `make([]int, 0, size)` 预先分配容量，避免多次扩容
- Go 没有 `while`，用 `for len(queue) > 0` 代替

## Go 与 PHP 差异总结

| 对比项 | PHP | Go |
|--------|-----|-----|
| **队列实现** | `array_shift()` 出队（O(n)）或 `SplQueue` | `queue[1:]` 切片出队（O(1)） |
| **nil/null 判断** | `$root === null` | `root == nil` |
| **成员访问** | `$node->val`（`->` 箭头语法） | `node.Val`（`.` 点语法，**大写**表示导出字段） |
| **空返回** | `return []` | `return [][]int{}` |
| **预分配切片** | 不支持，PHP 数组自动扩容 | `make([]int, 0, size)` 预分配容量 |
| **结构体定义** | 无，用 stdClass 或数组 | 明确的 `TreeNode` struct，字段需大写（公开） |

### 经典踩坑点

1. **Go 结构体字段大写问题：** `TreeNode` 的字段是 `Val`、`Left`、`Right`（首字母大写），因为它们需要是**导出字段**（public）。如果用 `node.val` 小写访问，编译会报错。

2. **Go 切片出队写法：** `queue = queue[1:]` 只是移动了切片的起始指针，底层数组没有被复制，性能好。但这意味着如果底层数组一直被引用，原头部元素不会立即被 GC 回收。在普通算法题中不用担心这个问题。

3. **PHP 中 `array_shift` 的隐性成本：** `array_shift()` 移除数组第一个元素后，会**重新索引所有数字键**，导致 O(n) 的时间复杂度。如果队列很大，使用 `SplQueue` 是更好的选择：
   ```php
   $queue = new SplQueue();
   $queue->enqueue($root);
   $node = $queue->dequeue();
   ```

4. **Go `make` 预分配容量：** `make([]int, 0, size)` 创建长度为 0、容量为 `size` 的切片，后续 `append` 在不超过 `size` 时不会触发扩容，略微提升性能。不强制要求，但面试写出来是加分项。

#leetcode #二叉树 #BFS #层序遍历 #队列
