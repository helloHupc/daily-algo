
**题目链接**：[https://leetcode.cn/problems/intersection-of-two-linked-lists/description](https://leetcode.cn/problems/intersection-of-two-linked-lists/description)

---

## 题目描述

给你两个单链表的头节点 `headA` 和 `headB`，请你找出并返回两个单链表相交的起始节点。如果两个链表不存在相交节点，返回 `null`。

注意，函数返回结果后，链表必须保持其原始结构。

**示例 1：**

```
输入：intersectVal = 8, listA = [4,1,8,4,5], listB = [5,6,1,8,4,5], skipA = 2, skipB = 3
输出：Intersected at '8'
解释：相交节点的值为 8。
```

**示例 2：**

```
输入：intersectVal = 2, listA = [1,9,1,2,4], listB = [3,2,4], skipA = 3, skipB = 1
输出：Intersected at '2'
```

**示例 3：**

```
输入：intersectVal = 0, listA = [2,6,4], listB = [1,5], skipA = 3, skipB = 2
输出：No intersection
解释：两个链表不相交，返回 null。
```

**提示：**

- `listA` 中节点数目为 `m`
- `listB` 中节点数目为 `n`
- `1 <= m, n <= 3 * 10^4`
- `1 <= Node.val <= 10^5`
- `0 <= skipA <= m`
- `0 <= skipB <= n`

**进阶：** 你能否设计一个时间复杂度 `O(m + n)`、仅用 `O(1)` 内存的解决方案？

## 解题思路

### 朴素解法：双循环

思路：遍历链表 A 的每个节点，对每个节点遍历链表 B 的所有节点，找到相同的节点。

**时间复杂度：** O(m × n)

**空间复杂度：** O(1)

**问题：**
- 效率太低，链表很长时会超时
- 需要注意不要改变原链表结构（要用新指针遍历）

### 进阶解法：双指针

核心思想：**走完自己的路，再走对方的路。**

```
A: a1 → a2 → c1 → c2
B: b1 → b2 → b3 → c1 → c2
```

指针 pA 走完链表 A，继续走链表 B
指针 pB 走完链表 B，继续走链表 A

**原理：**

```
A 长度 = a，B 长度 = b，相交部分长度 = c

pA 走的总路程 = a + (b - c) + c = a + b
pB 走的总路程 = b + (a - c) + c = a + b

总路程相同！
```

利用数学交换律 `a + b = b + a`：
- 如果相交：走 `a + b` 会在交点相遇
- 如果不相交：走 `a + b` 会同时走到 `null`

**时间复杂度：** O(m + n)

**空间复杂度：** O(1)

## Go 解法

```go
/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */
func getIntersectionNode(headA, headB *ListNode) *ListNode {
    pA := headA
    pB := headB
    for pA != pB {
        if pA == nil {
            pA = headB
        } else {
            pA = pA.Next
        }
        if pB == nil {
            pB = headA
        } else {
            pB = pB.Next
        }
    }
    return pA
}
```

**说明：** 使用双指针，pA 走完 A 后换到 B，pB 走完 B 后换到 A。走的总长度相同，要么在交点相遇，要么同时到 null。

## PHP 解法

### 朴素解法：双循环

```php
/**
 * Definition for a singly-linked list.
 * class ListNode {
 *     public $val = 0;
 *     public $next = null;
 *     function __construct($val) { $this->val = $val; }
 * }
 */
class Solution {
    /**
     * @param ListNode $headA
     * @param ListNode $headB
     * @return ListNode
     */
    function getIntersectionNode($headA, $headB) {
        $pA = $headA;
        while ($pA != null) {
            $pB = $headB;  // 每次内层循环重置
            while ($pB != null) {
                if ($pA === $pB) {  // 用 === 比较对象引用
                    return $pA;
                }
                $pB = $pB->next;
            }
            $pA = $pA->next;
        }
        return null;
    }
}
```

**说明：** 双循环是最朴素的想法，但时间复杂度 O(m×n)，效率较低。注意要用新指针遍历，不要改变原链表。

### 进阶解法：双指针

```php
/**
 * Definition for a singly-linked list.
 * class ListNode {
 *     public $val = 0;
 *     public $next = null;
 *     public function __construct($val = 0, $next = null) {
 *         $this->val = $val;
 *         $this->next = $next;
 *     }
 * }
 */
class Solution {

    /**
     * @param ListNode $headA
     * @param ListNode $headB
     * @return ListNode
     */
    function getIntersectionNode($headA, $headB) {
        $pA = $headA;
        $pB = $headB;
        while ($pA != $pB) {
            $pA = ($pA == null) ? $headB : $pA->next;
            $pB = ($pB == null) ? $headA : $pB->next;
        }
        return $pA;
    }
}
```

**说明：** 双指针解法利用数学交换律 `a + b = b + a`。两个指针走的总长度相同，要么在交点相遇，要么同时到 null。时间复杂度 O(m+n)，空间复杂度 O(1)。

#leetcode #链表 #双指针
