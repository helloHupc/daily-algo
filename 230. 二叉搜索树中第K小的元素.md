
**题目链接**：[https://leetcode.cn/problems/kth-smallest-element-in-a-bst/description](https://leetcode.cn/problems/kth-smallest-element-in-a-bst/description)

---
## 题目描述

给定一个二叉搜索树的根节点 `root`，和一个整数 `k`，请你设计一个算法查找其中第 `k` 小的元素（`k` 从 1 开始计数）。

**示例 1：**

```
输入：root = [3,1,4,null,2], k = 1
输出：1
```

**示例 2：**

```
输入：root = [5,3,6,2,4,null,null,1], k = 3
输出：3
```

**提示：**

- 树中的节点数为 `n`
- `1 <= k <= n <= 10^4`
- `0 <= Node.val <= 10^4`

**进阶：** 如果二叉搜索树经常被修改（插入/删除操作）并且你需要频繁地查找第 `k` 小的值，你将如何优化算法？

## 解题思路

### 核心思想：BST 中序遍历 = 有序序列

**BST 的核心性质**：中序遍历（左→根→右）得到的是一个**升序序列**。

所以找第 k 小的元素，本质上就是**中序遍历到第 k 个节点时停下**。

```
      ⑤
     /  \
    ③    ⑥
   /  \
  ②    ④
 /
①

中序遍历顺序：① → ② → ③ → ④ → ⑤ → ⑥
              1st  2nd  3rd  4th  5th  6th
```

k=3 → 节点 ③

### 解法一：递归中序遍历（带提前终止）

**思路：**
- 中序遍历 BST，每访问一个节点，计数器 `count` +1
- 当 `count == k` 时，当前节点就是答案，记录结果并终止后续遍历

**关键技巧：用返回值或引用来提前终止递归**，而不是傻傻遍历完整个树。

### 解法二：迭代中序遍历（栈）

**思路：**
- 用栈模拟中序遍历，不需要递归
- 每次弹出栈顶元素时计数，达到 k 时直接返回

### 进阶思路：记录子树节点数

如果 BST 会被频繁修改和频繁查询第 k 小：
- 在每个节点上额外存储**左子树的节点数**（或该节点为根的子树大小）
- 插入/删除时 O(log n) 维护这个信息
- 查询第 k 小时根据左子树节点数决定往左走还是往右走：
  - `leftSize >= k` → 去左子树找
  - `leftSize + 1 == k` → 当前节点就是答案
  - `leftSize + 1 < k` → 去右子树找，`k = k - leftSize - 1`
- 查询时间复杂度从 O(n) 降到 O(log n)

不过在本题中树本身没有这个信息，所以这里只讲前两种解法。

### 复杂度分析

| 解法 | 时间复杂度 | 空间复杂度 |
|------|-----------|-----------|
| 递归中序（提前终止） | O(n) | O(h) — 递归栈 |
| 迭代中序（栈） | O(n) | O(h) — 栈空间 |
| 进阶（记录子树大小） | O(log n) | O(n) |

## PHP 解法

```php
class Solution {

    /**
     * @param TreeNode $root
     * @param Integer $k
     * @return Integer
     */
    function kthSmallest($root, $k) {
        $count = 0;
        $result = null;

        $this->inorder($root, $k, $count, $result);

        return $result;
    }

    /**
     * 中序遍历，找到第 k 个节点后提前终止
     */
    private function inorder($node, $k, &$count, &$result) {
        if ($node === null || $result !== null) {
            return; // 找到结果后提前终止
        }

        // 左
        $this->inorder($node->left, $k, $count, $result);

        // 根
        $count++;
        if ($count === $k) {
            $result = $node->val;
            return;
        }

        // 右
        $this->inorder($node->right, $k, $count, $result);
    }
}
```

**说明：**
- 用**引用传参** `&$count` 和 `&$result` 在递归调用间共享状态
- 每次递归前检查 `$result !== null`，找到答案后立刻返回，不再继续遍历
- 中序遍历框架：左→根→右，根节点部分做计数和判断

**迭代写法（栈）：**

```php
class Solution {

    /**
     * @param TreeNode $root
     * @param Integer $k
     * @return Integer
     */
    function kthSmallest($root, $k) {
        $stack = [];
        $cur = $root;
        $count = 0;

        while ($cur !== null || !empty($stack)) {
            // 一直往左走
            while ($cur !== null) {
                $stack[] = $cur;
                $cur = $cur->left;
            }

            // 弹出栈顶
            $cur = array_pop($stack);
            $count++;

            if ($count === $k) {
                return $cur->val;
            }

            // 往右走
            $cur = $cur->right;
        }

        return -1;
    }
}
```

## Go 解法

```go
/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */

func kthSmallest(root *TreeNode, k int) int {
    count := 0
    result := 0
    inorder(root, k, &count, &result)
    return result
}

func inorder(node *TreeNode, k int, count *int, result *int) {
    if node == nil {
        return
    }

    // 左
    inorder(node.Left, k, count, result)

    // 根：如果已经在左子树找到了结果，这里不会再执行后续递归
    *count++
    if *count == k {
        *result = node.Val
        return
    }

    // 右
    inorder(node.Right, k, count, result)
}
```

**说明：**
- Go 没有引用传参，用**指针** `*int` 来模拟共享状态
- `*count` 和 `*result` 需要通过解引用来读写
- 递归的终止条件：找到结果后 `return`，但仍然会回退到上一层递归，但不会再产生新的递归调用

**迭代写法（栈）：**

```go
func kthSmallest(root *TreeNode, k int) int {
    stack := make([]*TreeNode, 0)
    cur := root
    count := 0

    for cur != nil || len(stack) > 0 {
        // 一直往左走
        for cur != nil {
            stack = append(stack, cur)
            cur = cur.Left
        }

        // 弹出栈顶
        cur = stack[len(stack)-1]
        stack = stack[:len(stack)-1]

        count++
        if count == k {
            return cur.Val
        }

        // 往右走
        cur = cur.Right
    }

    return -1
}
```

## Go 与 PHP 差异总结

| 对比项 | PHP | Go |
|--------|-----|-----|
| **共享状态方式** | 引用传参 `&$count` | 指针传参 `*int` + 解引用 `*count` |
| **提前终止** | 检查 `$result !== null` | 检查 `*count == k` 后 return |
| **嵌套函数** | 需要用 `private function` 方法 | 可以用包级函数或闭包 |
| **栈操作** | `$stack[] = $cur` 入栈，`array_pop()` 出栈 | `append()` 入栈，切片操作 `stack[:len-1]` 出栈 |
| **for 循环** | `while` 加内部 `while` | `for` 加内部 `for` |

### 关于 Go 中 `*int` 指针传参的说明

对于刚从 PHP 转 Go 的开发者来说，这是一个重要差异：

```php
// PHP：直接用 & 引用
function inorder($node, $k, &$count, &$result) {
    $count++;           // 直接操作
    $result = $node->val; // 直接操作
}
```

```go
// Go：需要用指针
func inorder(node *TreeNode, k int, count *int, result *int) {
    *count++              // 解引用
    *result = node.Val    // 解引用
}
```

Go 中**所有参数都是值传递**（包括指针本身，但指针的值是地址，所以能修改原变量）。这里的 `count` 是一个 `*int` 类型的参数，它本身是一个指针变量的副本，但这个副本指向的地址和原变量相同。

## 扩展知识

### 为什么不能简单用变量返回值来实现？

你可能想这么写：

```go
func kthSmallest(root *TreeNode, k int) int {
    count := 0
    result := 0
    inorder(root, k, count, result) // 值传递，改不了原来的变量！
    return result                    // 永远是 0
}
```

Go 中 `int` 是值类型，传给函数会**复制一份**，在函数内部修改不影响外部。这就是为什么需要 `*int` 指针。

### 进阶问题详解

如果 BST 经常被修改，又需要频繁查第 k 小，可以在**插入/删除时维护子树大小**：

```go
type TreeNodeWithSize struct {
    Val    int
    Left   *TreeNodeWithSize
    Right  *TreeNodeWithSize
    Size   int // 以该节点为根的子树中节点总数
}

func kthSmallest(root *TreeNodeWithSize, k int) int {
    cur := root
    for cur != nil {
        leftSize := 0
        if cur.Left != nil {
            leftSize = cur.Left.Size
        }
        if k <= leftSize {
            cur = cur.Left
        } else if k == leftSize + 1 {
            return cur.Val
        } else {
            k -= leftSize + 1
            cur = cur.Right
        }
    }
    return -1
}
```

这样查询复杂度降到 O(log n)，插入/删除也是 O(log n) 维护 Size。

#leetcode #二叉树 #BST #中序遍历 #栈
