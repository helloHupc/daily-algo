
**题目链接**：[https://leetcode.cn/problems/sliding-window-maximum/description](https://leetcode.cn/problems/sliding-window-maximum/description)

---

## 题目描述

给定一个整数数组 `nums`，有一个大小为 `k` 的滑动窗口从数组的最左侧移动到最右侧。只可以看到在滑动窗口内的 `k` 个数字，滑动窗口每次只向右移动一位。返回滑动窗口中的最大值。

**示例：**

```
输入: nums = [1,3,-1,-3,5,3,6,7], k = 3
输出: [3,3,5,5,6,7]

解释:
滑动窗口的位置                最大值
---------------               -----
[1  3  -1] -3  5  3  6  7       3
 1 [3  -1  -3] 5  3  6  7       3
 1  3 [-1  -3  5] 3  6  7       5
 1  3  -1 [-3  5  3] 6  7       5
 1  3  -1  -3 [5  3  6] 7       6
 1  3  -1  -3  5 [3  6  7]      7
```

---

## 暴力解法（超时）

### 思路

每个窗口遍历 `k` 个元素，找出最大值。

### 代码

```go
func maxSlidingWindow(nums []int, k int) []int {
    left := 0
    right := k - 1
    n := len(nums)
    result := []int{}

    for right < n {
        max := nums[left]
        for step := left; step <= right; step++ {
            if nums[step] > max {
                max = nums[step]
            }
        }
        result = append(result, max)
        left++
        right++
    }
    return result
}
```

### 为什么超时？

| 指标 | 数值 |
|------|------|
| 窗口个数 | `n - k + 1` |
| 每窗口操作 | 遍历 `k` 个元素 |
| 总时间复杂度 | **O(n × k)** |

当 `n = 10^5`, `k = 5 × 10^4` 时：

```
操作次数 ≈ 10^5 × 5 × 10^4 = 5 × 10^9
```

**50 亿次操作**，远超一般 OJ 的 1 秒限制。

### 核心问题

每个窗口都**重新扫描**所有元素，前面窗口已经比较过的信息全部丢弃，做了大量重复工作。

---

## 优化思路：单调队列

### 核心观察

```
窗口1: [1, 3, -1]      最大值 = 3
窗口2:    [3, -1, -3]   最大值 = 3  ← 3 还在窗口里，可以直接复用！
窗口3:       [-1, -3, 5] 最大值 = 5  ← 3 不在了，但 -1, -3 比 5 小，也没必要保留
```

**关键洞察**：如果窗口中有 `a < b` 且 `a` 在 `b` 的左边，那么 `a` 永远不可能成为最大值（因为 `b` 比 `a` 大，且 `b` 后出窗口）。

### 类比理解

想象一个"VIP 通道"：

- **暴力解法**：每进来一个新球，把桶里所有球翻出来比一遍
- **单调队列**：维护一个按实力排序的通道，弱的直接淘汰，永远不用再比

### 队列规则

1. **入队前**：从队尾弹出所有小于当前元素的（它们没机会了）
2. **入队**：当前元素加入队尾
3. **检查队头**：如果队头已滑出窗口，移除
4. **取最大值**：队头就是当前窗口最大值

---

## 单调队列解法

### Go 代码

```go
func maxSlidingWindow(nums []int, k int) []int {
    n := len(nums)
    result := make([]int, 0, n-k+1)
    deque := make([]int, 0) // 存下标，对应值从队头到队尾递减

    for i := 0; i < n; i++ {
        // 1. 移除队尾所有小于当前元素的下标
        for len(deque) > 0 && nums[deque[len(deque)-1]] <= nums[i] {
            deque = deque[:len(deque)-1]
        }

        // 2. 当前下标入队
        deque = append(deque, i)

        // 3. 移除已滑出窗口的队头
        if deque[0] <= i-k {
            deque = deque[1:]
        }

        // 4. 窗口形成后，队头就是最大值
        if i >= k-1 {
            result = append(result, nums[deque[0]])
        }
    }
    return result
}
```

### PHP 代码

```php
function maxSlidingWindow(array $nums, int $k): array {
    $n = count($nums);
    $result = [];
    $deque = new SplDoublyLinkedList(); // 双端队列，存下标

    for ($i = 0; $i < $n; $i++) {
        // 1. 移除队尾所有小于当前元素的下标
        while (!$deque->isEmpty() && $nums[$deque->top()] <= $nums[$i]) {
            $deque->pop();
        }

        // 2. 当前下标入队
        $deque->push($i);

        // 3. 移除已滑出窗口的队头
        if ($deque->bottom() <= $i - $k) {
            $deque->shift();
        }

        // 4. 窗口形成后，队头就是最大值
        if ($i >= $k - 1) {
            $result[] = $nums[$deque->bottom()];
        }
    }

    return $result;
}
```

### 执行过程图解

以 `nums = [1,3,-1,-3,5,3,6,7], k = 3` 为例：

| i | nums[i] | 操作 | deque（存下标） | 对应值 | 窗口最大值 |
|---|---------|------|----------------|--------|-----------|
| 0 | 1 | 入队 | [0] | [1] | - |
| 1 | 3 | 弹出0，入队1 | [1] | [3] | - |
| 2 | -1 | 保留，入队2 | [1,2] | [3,-1] | **3** |
| 3 | -3 | 保留，入队3 | [1,2,3] | [3,-1,-3] | **3** |
| 4 | 5 | 弹出3,2,1，入队4 | [4] | [5] | **5** |
| 5 | 3 | 保留，入队5 | [4,5] | [5,3] | **5** |
| 6 | 6 | 弹出5，入队6 | [4,6] | [5,6] | **6** |
| 7 | 7 | 弹出6,4，入队7 | [7] | [7] | **7** |

---

## 复杂度分析

### 时间复杂度：O(n)

| 操作 | 次数 | 说明 |
|------|------|------|
| 入队（append） | n 次 | 每个元素最多入队1次 |
| 出队（队尾弹出） | ≤ n 次 | 每个元素最多被弹出1次 |
| 队头移除 | ≤ n 次 | 每个元素最多被移除1次 |
| **总计** | **≤ 3n 次** | **O(n)** |

**关键**：虽然有两层循环，但内层循环所有轮次加起来最多执行 `n` 次（每个元素最多被弹出一次）。

### 空间复杂度：O(k)

队列最多存储 `k` 个元素。

---

## 两种解法对比

| 对比项 | 暴力解法 | 单调队列 |
|--------|---------|---------|
| 时间复杂度 | O(n × k) | **O(n)** |
| 空间复杂度 | O(1) | O(k) |
| 核心思想 | 每个窗口独立扫描 | 维护"可能成为最大值"的元素 |
| n=10^5, k=5×10^4 | ~5×10^9 次操作 ❌ | ~3×10^5 次操作 ✅ |

---

## 核心要点总结

1. **单调队列的本质**：队列中的元素值从队头到队尾**单调递减**，所以队头永远是当前窗口最大值

2. **为什么高效**：每个元素最多入队、出队各一次，总操作 **O(n)**，与窗口大小 `k` 无关

3. **维护两个性质**：
   - **单调性**：队尾弹出所有小于新元素的，保证递减
   - **窗口合法性**：队头检查是否滑出窗口

4. **适用场景**：滑动窗口最值问题、需要 O(1) 获取区间最值的场景

#leetcode #滑动窗口 #单调队列 #队列
