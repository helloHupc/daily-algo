
**题目链接**：[https://leetcode.cn/problems/is-subsequence/description](https://leetcode.cn/problems/is-subsequence/description)

---

## 题目描述

给定字符串 `s` 和 `t`，判断 `s` 是否为 `t` 的子序列。

字符串的一个子序列是原始字符串删除一些（也可以不删除）字符而不改变剩余字符相对位置形成的新字符串。（例如，`"ace"` 是 `"abcde"` 的一个子序列，而 `"aec"` 不是）。

**进阶：**

如果有大量输入的 S，称作 S1, S2, ... , Sk 其中 k >= 10亿，你需要依次检查它们是否为 T 的子序列。在这种情况下，你会怎样改变代码？

**示例 1：**

```
输入：s = "abc", t = "ahbgdc"
输出：true
```

**示例 2：**

```
输入：s = "axc", t = "ahbgdc"
输出：false
```

**提示：**

- `0 <= s.length <= 100`
- `0 <= t.length <= 10^4`
- 两个字符串都只由小写字符组成。

## 解题思路

### 基础解法：双指针

用两个指针 `i` 和 `j`，分别指向 `s` 和 `t`：

- 遍历 `t`，当 `s[i] == t[j]` 时，`i` 往前走
- 遍历结束后，如果 `i` 走到了 `s` 的末尾，说明 `s` 是 `t` 的子序列

**时间复杂度：** O(len(s) + len(t))

**空间复杂度：** O(1)

### 进阶解法：预处理 + 二分查找

如果有大量 `s`（10亿个）要判断，每次都遍历 `t` 太慢。

思路：**预处理 `t`，记录每个字符出现的位置列表，然后对每个 `s` 用二分查找快速判断。**

- 预处理：遍历 `t`，将每个字符出现的所有位置存入 map，位置列表天然有序
- 查找：对 `s` 中每个字符，在其位置列表中二分查找"第一个大于当前位置"的位置
- 如果某个字符在 `t` 中不存在，或者找不到比当前位置更大的位置，则不是子序列

**时间复杂度：** 预处理 O(len(t))，每次判断 O(len(s) * log(len(t)))

**空间复杂度：** O(len(t))

## Go 解法

### 基础解法：双指针

```go
func isSubsequence(s string, t string) bool {
    i, j := 0, 0
    for i < len(s) && j < len(t) {
        if s[i] == t[j] {
            i++
        }
        j++
    }
    return i == len(s)
}
```

### 进阶解法：预处理 + 二分查找

```go
func isSubsequence(s string, t string) bool {
    // 预处理：记录每个字符在 t 中出现的位置
    tmap := make(map[byte][]int)
    for i := 0; i < len(t); i++ {
        tmap[t[i]] = append(tmap[t[i]], i)
    }

    // 对 s 中每个字符，在 t 的位置列表中二分查找
    cur := -1
    for i := 0; i < len(s); i++ {
        indices, ok := tmap[s[i]]
        if !ok {
            return false  // t 中没有这个字符
        }
        // 二分查找：找第一个大于 cur 的位置
        j := binarySearch(indices, cur)
        if j == len(indices) {
            return false  // 没有比 cur 更大的位置了
        }
        cur = indices[j]
    }
    return true
}

func binarySearch(arr []int, target int) int {
    left, right := 0, len(arr)
    for left < right {
        mid := left + (right - left) / 2
        if arr[mid] > target {
            right = mid
        } else {
            left = mid + 1
        }
    }
    return left
}
```

**说明：** 预处理和查找都使用 `byte` 类型（`for i` + `s[i]`/`t[i]`），保持类型一致。二分查找使用半开区间 `[left, right)`，找"第一个大于 target 的位置"，循环结束时 `left` 即为答案。

## PHP 解法

### 基础解法：双指针

```php
class Solution {

    /**
     * @param String $s
     * @param String $t
     * @return Boolean
     */
    function isSubsequence($s, $t) {
        $i = 0;
        $j = 0;
        $lenS = strlen($s);
        $lenT = strlen($t);

        while ($i < $lenS && $j < $lenT) {
            if ($s[$i] == $t[$j]) {
                $i++;
            }
            $j++;
        }

        return $i == $lenS;
    }
}
```

### 进阶解法：预处理 + 二分查找

```php
class Solution {

    /**
     * @param String $s
     * @param String $t
     * @return Boolean
     */
    function isSubsequence($s, $t) {
        // 预处理：记录每个字符在 t 中出现的位置
        $tmap = [];
        $lenT = strlen($t);
        for ($i = 0; $i < $lenT; $i++) {
            $tmap[$t[$i]][] = $i;
        }

        // 对 s 中每个字符，在 t 的位置列表中二分查找
        $cur = -1;
        $lenS = strlen($s);
        for ($i = 0; $i < $lenS; $i++) {
            $ch = $s[$i];
            if (!isset($tmap[$ch])) {
                return false;  // t 中没有这个字符
            }
            $indices = $tmap[$ch];
            // 二分查找：找第一个大于 cur 的位置
            $j = $this->binarySearch($indices, $cur);
            if ($j == count($indices)) {
                return false;  // 没有比 cur 更大的位置了
            }
            $cur = $indices[$j];
        }
        return true;
    }

    function binarySearch($arr, $target) {
        $left = 0;
        $right = count($arr);
        while ($left < $right) {
            $mid = intdiv($left + $right, 2);
            if ($arr[$mid] > $target) {
                $right = $mid;
            } else {
                $left = $mid + 1;
            }
        }
        return $left;
    }
}
```

**说明：** PHP 中整数除法使用 `intdiv()` 避免浮点数问题。预处理阶段 `$tmap[$t[$i]][] = $i` 利用 PHP 数组自动追加的特性，无需判断键是否存在。

#leetcode #双指针 #字符串 #动态规划
