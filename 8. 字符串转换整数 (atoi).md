
**题目链接**：[https://leetcode.cn/problems/string-to-integer-atoi/description/](https://leetcode.cn/problems/string-to-integer-atoi/description/)

---

## 题目描述

实现一个 `myAtoi(string s)` 函数，将字符串转换成一个 32 位有符号整数。

算法步骤：

1. **丢弃前导空格**
2. **确定符号**：检查 `-` 或 `+`，不存在则默认为正
3. **读入数字**：跳过前置零，遇到非数字字符或末尾停止。没读到数字则结果为 0
4. **范围截断**：小于 `-2^31` 返回 `-2^31`，大于 `2^31 - 1` 返回 `2^31 - 1`

**示例**

```
输入: "42"              输出: 42
输入: "   -042"         输出: -42
输入: "1337c0d3"        输出: 1337
输入: "0-1"             输出: 0
输入: "words and 987"   输出: 0
```

---

## 解题思路

这道题的核心是 **逐字符解析 + 溢出前检查**。

流程就是题目描述的四步：

```
跳过空格 → 读符号 → 解析数字直到非数字 → 溢出截断
```

### 溢出检测的原理

关键点在于：**不能在乘法之后判断溢出**，因为整数溢出后值会回绕（wrap around），结果不可控。必须在 **乘以 10 之前** 提前判断。

当前结果是 `res`，即将读取的数字是 `digit`：

```
res * 10 + digit > MaxInt32    ← 溢出条件
→ res * 10 > MaxInt32 - digit
→ res > (MaxInt32 - digit) / 10
```

```
res * 10 + digit < MinInt32    ← 下溢条件
→ res * 10 < MinInt32 + digit
→ res < (MinInt32 + digit) / 10
```

但这里有一个简化技巧：我们统一按 **正数** 累加数字，最后再乘符号。这样只需要判断正溢出，溢出时根据符号返回 `MaxInt32` 或 `MinInt32` 即可。

---

## PHP 解法

先用熟悉的 PHP 把算法逻辑过一遍。

```php
function myAtoi(string $s): int {
    $i = 0;
    $n = strlen($s);

    // 第 1 步：跳过前导空格
    while ($i < $n && $s[$i] === ' ') {
        $i++;
    }
    if ($i === $n) {
        return 0;
    }

    // 第 2 步：处理符号
    $sign = 1;
    if ($s[$i] === '-') {
        $sign = -1;
        $i++;
    } elseif ($s[$i] === '+') {
        $i++;
    }

    // 第 3 步：读取数字
    $result = 0;
    $maxInt = 2147483647;    // 2^31 - 1
    $minInt = -2147483648;   // -2^31

    while ($i < $n && ctype_digit($s[$i])) {
        $digit = intval($s[$i]);

        // 第 4 步：溢出检查（在乘 10 之前）
        if ($result > intdiv($maxInt - $digit, 10)) {
            return $sign === 1 ? $maxInt : $minInt;
        }

        $result = $result * 10 + $digit;
        $i++;
    }

    return $result * $sign;
}
```

### PHP 关键点说明

**1. `ctype_digit` 判断数字字符**

```php
ctype_digit('5')  // true
ctype_digit('a')  // false
```

也可以写 `$s[$i] >= '0' && $s[$i] <= '9'`，效果一样，但 `ctype_digit` 更简洁。

**2. `intdiv` 整数除法**

PHP 的 `/` 会返回 float，`intdiv` 是 PHP 7 引入的整数除法函数，返回 int。

```php
intdiv(7, 3)   // 2
7 / 3          // 2.3333...（float）
```

**3. 为什么 `$maxInt - $digit` 不会变成 float？**

两个 int 相减在 PHP 中还是 int，不会像 `/` 那样自动转 float。

---

## Go 解法

同样的逻辑，看看 Go 里怎么写。

```go
import "math"

func myAtoi(s string) int {
    i := 0
    n := len(s)

    // 第 1 步：跳过前导空格
    for i < n && s[i] == ' ' {
        i++
    }
    if i == n {
        return 0
    }

    // 第 2 步：处理符号
    sign := 1
    if s[i] == '-' {
        sign = -1
        i++
    } else if s[i] == '+' {
        i++
    }

    // 第 3 步：读取数字并转换
    result := 0
    for i < n && s[i] >= '0' && s[i] <= '9' {
        digit := int(s[i] - '0')

        // 第 4 步：溢出检查（在乘 10 之前）
        if result > (math.MaxInt32-digit)/10 {
            if sign == 1 {
                return math.MaxInt32
            }
            return math.MinInt32
        }

        result = result*10 + digit
        i++
    }

    return result * sign
}
```

### Go 关键点说明

**1. 为什么 `s[i]` 是 `byte` 不是 `rune`？**

Go 中通过索引访问字符串得到的是 `byte`（即 `uint8`），不是 `rune`。因为字符串底层是字节数组。而 `for range` 遍历得到的才是 `rune`。

**2. 为什么 `digit` 要显式 `int()`？**

```go
digit := int(s[i] - '0')
```

`s[i] - '0'` 的结果是 `byte` 类型（`byte - byte → byte`），值范围 0~9。但后面跟 `int` 类型做运算（`result * 10 + digit`）时，Go 不允许不同类型的数值直接运算，所以必须转成 `int`。

如果不转，`result = result*10 + digit` 会编译报错——`int` 和 `byte` 不能直接相加。

**3. 判断数字字符为什么是 `s[i] >= '0'` 而不是 `i >= '0'`？**

这是新手最容易犯的错。`'0'` 是字符，值 48（ASCII 码）。如果写成：

```go
// ❌ 错误写法
for i < n && i >= '0' && i <= '9' {
```

就变成了 `i >= 48 && i <= 57`——拿索引去跟 48、57 比了，编译能过（因为 `i` 是 `int`，`'0'` 是无类型常量会按 `int` 解析），但逻辑完全不对。

---

## Go 与 PHP 差异总结

### 类型系统

| 场景      | PHP                             | Go                                   |
| ------- | ------------------------------- | ------------------------------------ |
| 字符串索引类型 | `string`（按字节访问，返回 string 中的单字符） | `byte`（uint8）                        |
| 字符字面量   | `'0'` 是字符串，可跟字符串直接比较            | `'0'` 是 rune（int32），但作为无类型常量可自动适配    |
| 字符串字面量  | `"0"` 和 `'0'` 都是字符串             | `"0"` 是 string，`'0'` 是 rune，**完全不同** |
| 类型转换    | 弱类型，自动隐式转换                      | 强类型，必须显式转换                           |

### 语法差异

| 对比项 | Go | PHP |
|------|----|-----|
| 循环 | `for i < n` | `while ($i < $n)` |
| 变量声明 | `var i int` 或 `i := 0` | `$i = 0` |
| 整数除法 | 直接 `/`（int 相除返回 int） | `intdiv($a, $b)`，否则返回 float |
| 字符判断 | `s[i] >= '0' && s[i] <= '9'` | `ctype_digit($s[$i])` 或 `$s[$i] >= '0'` |
| 字符转数字 | `int(s[i] - '0')` | `intval($s[$i])` |
| 整数边界常量 | `math.MaxInt32` / `math.MinInt32`（内置） | 手写常量 |

### 经典踩坑点（这次实战中遇到的）

**坑 1：Go 的 `i` 和 `s[i]` 都是整数，混写不报错**

```go
// 本意是 s[i] >= '0'，结果写了 i >= '0'
for i < n && i >= '0' && i <= '9' {   // 编译通过！但逻辑全错
```

因为 `i` 和 `'0'` 都是整数类型（`int` 和 无类型常量），Go 允许它们比较，bug 被静默消化了。

**坑 2：PHP 的字符串与整数比较规则**

```php
// 本意是 $s[$i] >= '0'，结果写了 $s[$i] >= 0
while ($i < $n && $s[$i] >= 0 && $s[$i] <= 9) {  // 非数字字符也进了循环
```

PHP 字符串跟整数比较时，会把字符串转成整数再比，`'w'` 转成 0，导致字母也通过判断。

**坑 3：PHP 变量名写错**

```php
// 本意是 $s[$i] == '+'，结果写了 $sign == '+'
} elseif ($sign == '+') {
    $i++;
}
```

`$sign` 此时是 `1`，永远不等于 `'+'`，所以 `+` 号永远不会被跳过。

**坑 4：Go 的 `for` vs `if` 跳过空格**

```go
// 应该用 for 循环跳过所有连续空格
for i < n && s[i] == ' ' {   // ✅ 正确
    i++
}

// 如果写成 if，只跳过一个空格
if i < n && s[i] == ' ' {    // ❌ 多个空格时出错
    i++
}
```

---

## 复杂度分析

| | 时间 | 空间 |
|---|------|------|
| **PHP** | O(n) | O(1) |
| **Go** | O(n) | O(1) |

只遍历字符串一次，只用了常数个变量。

---

## 扩展知识

### Go 的 `math` 常量

```go
math.MaxInt32  // 2147483647
math.MinInt32  // -2147483648
math.MaxInt64  // 9223372036854775807
math.MinInt64  // -9223372036854775808
```

注意 Go 的 `int` 类型在 32 位系统上是 32 位，在 64 位系统上是 64 位。如果要确保 32 位截断行为，必须用 `math.MaxInt32` 而不是 `math.MaxInt`。

### `math.MaxInt32` 到底是什么类型？

它是**无类型常量**（untyped constant），不是 `int32`。

```go
const MaxInt32 = 1<<31 - 1   // 无类型整数常量
```

因为它无类型，所以使用时可以根据上下文自动适配类型。跟 `int` 一起运算时就按 `int` 处理：

```go
var result int = 0
result > (math.MaxInt32 - digit) / 10   // math.MaxInt32 在此处视为 int
```

### PHP 没有整数边界常量

PHP 没有类似 `MaxInt32` 的内置常量，需要手写：

```php
$maxInt = 2147483647;
$minInt = -2147483648;
```

在 64 位系统下 PHP 的 `int` 是 64 位的，这两个值都在范围内，所以没问题。但如果把这些常量用在 32 位 PHP 系统上，`2147483647` 仍然是 `int`（32 位符号 int 的最大值），而 `-2147483648` 会变成 float，需要注意。

---

## 易错点总结

1. **前置零不需要特殊处理**：`result = result * 10 + digit` 自然吞掉前导零（`0 * 10 + 0 = 0`）
2. **溢出必须在乘法前检测**：乘完再判断，值已经回绕了
3. **符号处理**：`"+-12"` 中 `+` 之后的 `-` 不是符号，是非法字符，应返回 0
4. **空格后的中断**：`"   +0 123"` 读到 `0` 后面的空格时停止，结果是 `0`，不是 `123`
5. **空字符串或全空格**：跳过空格后如果到末尾，返回 `0`

#leetcode #字符串 #数学 #溢出处理
