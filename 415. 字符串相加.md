
**题目链接**：[https://leetcode.cn/problems/add-strings/description](https://leetcode.cn/problems/add-strings/description)

---

## 题目描述

给定两个字符串形式的非负整数 `num1` 和 `num2`，计算它们的和并同样以字符串形式返回。

- 不能使用内建的大整数库（如 BigInteger）
- 不能直接将输入字符串转为整数

**示例**

```
输入: num1 = "456", num2 = "77"
输出: "533"

输入: num1 = "0", num2 = "0"
输出: "0"
```

---

## 解题思路

模拟竖式加法：

1. 用两个指针分别从 `num1` 和 `num2` 的**末尾**往左走
2. 每轮把对应位相加，加上进位 `carry`
3. 当前位 = `sum % 10`，新进位 = `sum / 10`
4. 最后如果还有进位，再补一位

示例：`456 + 77`

```
  4 5 6
+   7 7
-------
  5 3 3

第1轮: 6 + 7 = 13     → 位=3, 进位=1
第2轮: 5 + 7 + 1 = 13 → 位=3, 进位=1
第3轮: 4 + 0 + 1 = 5  → 位=5, 进位=0
```

---

## 一个常见疑问：字符怎么变成数字？

字符串里存的是字符，不是数字。把字符转数字的关键是：

```
字符 '0' 的 ASCII 码是 48
'0' ~ '9' 的 ASCII 码是 48 ~ 57（连续）
```

所以：

```go
'3' - '0' = 51 - 48 = 3   // 字符转数字
3 + '0'   = 3 + 48 = 51   // 数字转字符
```

在 Go 里，这一行就完成了"数字 → 字符 → byte"：

```go
byte(sum%10 + '0')
```

注意：`byte(3)` 不是字符 `'3'`，它对应的是 ASCII 码 3（一个不可打印控制字符）。**必须 `+ '0'` 才能得到正确的字符。**

---

## Go 解法

```go
func addStrings(num1 string, num2 string) string {
    i := len(num1) - 1
    j := len(num2) - 1
    carry := 0
    result := []byte{}

    for i >= 0 || j >= 0 || carry > 0 {
        sum := carry
        if i >= 0 {
            sum += int(num1[i] - '0')
            i--
        }
        if j >= 0 {
            sum += int(num2[j] - '0')
            j--
        }
        carry = sum / 10
        result = append(result, byte(sum%10+'0'))
    }

    // result 是从低位到高位拼的，需要反转
    for left, right := 0, len(result)-1; left < right; left, right = left+1, right-1 {
        result[left], result[right] = result[right], result[left]
    }

    return string(result)
}
```

### 为什么用 `[]byte` 而不是直接拼字符串？

Go 字符串是**不可变的**，直接用 `+` 拼接每次都会创建新字符串：

```go
result = string(rune(sum%10+'0')) + result   // 每次都复制，O(n)
// 执行 n 次后总复杂度：O(n²)
```

用 `[]byte` + `append` + 最后反转，总复杂度是 **O(n)**。

### 为什么最后要反转？

`append` 只能追加到**末尾**，而我们是从**低位到高位**拼的，所以顺序是反的：

```
计算顺序: 3 → 3 → 5
append:   ['3', '3', '5']   // 低位在前
反转后:   ['5', '3', '3']   // 正确顺序
```

### Go 切片操作复习

| 操作    | 写法                 | 位置  |
| ----- | ------------------ | --- |
| 追加到末尾 | `append(s, x)`     | 队尾  |
| 移除末尾  | `s = s[:len(s)-1]` | 队尾  |
| 移除开头  | `s = s[1:]`        | 队头  |

---

## PHP 解法

```php
function addStrings(string $num1, string $num2): string {
    $i = strlen($num1) - 1;
    $j = strlen($num2) - 1;
    $carry = 0;
    $result = '';

    while ($i >= 0 || $j >= 0 || $carry > 0) {
        $sum = $carry;
        if ($i >= 0) {
            $sum += intval($num1[$i]);
            $i--;
        }
        if ($j >= 0) {
            $sum += intval($num2[$j]);
            $j--;
        }
        $carry = intdiv($sum, 10);
        $result = ($sum % 10) . $result; // 直接拼到前面，不用反转
    }

    return $result;
}
```

### PHP 特有语法说明

| 语法 | 说明 |
|------|------|
| `intval($num1[$i])` | 字符转数字，类似 Go 的 `num1[i] - '0'` |
| `intdiv($sum, 10)` | 整数除法，PHP 的 `/` 会返回浮点数 |
| `$result = ($sum % 10) . $result` | 字符串拼接到前面，PHP 内部优化，比 Go 的 `+` 高效 |

### PHP 为什么可以直接拼到前面？

PHP 字符串是**可变的**，内部有优化机制，直接 `.=` 拼接不会像 Go 那样每次都复制整个字符串。所以 PHP 版本不需要反转，代码更简洁。

---

## 两种语言对比

| 对比项 | Go | PHP |
|------|----|-----|
| 字符串是否可变 | 不可变 | 可变 |
| 拼接效率 | `+` 是 O(n)，用 `[]byte` + `append` 是 O(1) | `.=` 是 O(1) |
| 是否需要反转 | 需要 | 不需要 |
| 字符转数字 | `num[i] - '0'` | `intval($num[$i])` |
| 整数除法 | `/` 直接返回 int | `intdiv($a, $b)` |

---

## 复杂度分析

|     | 时间           | 空间           |
| --- | ------------ | ------------ |
| Go  | O(max(m, n)) | O(max(m, n)) |
| PHP | O(max(m, n)) | O(max(m, n)) |

其中 `m` 和 `n` 分别是两个字符串的长度。

---

## 关键要点总结

1. **字符转数字**：`字符 - '0'`（Go）或 `intval()`（PHP）
2. **数字转字符**：`数字 + '0'`（Go）或 `(string)数字`（PHP）
3. **进位处理**：`sum / 10` 取进位，`sum % 10` 取当前位
4. **Go 的坑**：字符串不可变，循环拼接用 `[]byte` + `append` + 反转
5. **PHP 的坑**：`/` 返回浮点数，整除必须用 `intdiv()`

#leetcode #字符串 #数学
