
**题目链接**：[https://leetcode.cn/problems/longest-palindrome/description](https://leetcode.cn/problems/longest-palindrome/description)

---

## 题目描述

给定一个包含大写字母和小写字母的字符串 `s`，返回通过这些字母构造成的最长的回文串的长度。

在构造过程中，请注意区分大小写。比如 `"Aa"` 不能当做一个回文字符串。

**示例 1：**

```
输入: s = "abccccdd"
输出: 7
解释: 我们可以构造的最长的回文串是 "dccaccd"，它的长度是 7。
```

**示例 2：**

```
输入: s = "a"
输出: 1
解释: 可以构造的最长回文串是 "a"，它的长度是 1。
```

**提示：**

- `1 <= s.length <= 2000`
- `s` 只由小写 和/或 大写英文字母组成

## 解题思路

构造回文串的规则：

1. **偶数次数的字符**：全部可以用（左右对称放）
2. **奇数次数的字符**：取最大的偶数部分（`v / 2 * 2`），剩下的 1 个可以放中间
3. **中间只能放 1 个字符**：只要存在奇数次数的字符，最终长度可以 +1

**公式：**

```
长度 = 所有字符的 (v / 2 * 2) 的总和 + (是否有奇数次数的字符 ? 1 : 0)
```

**举个例子：**

```
s = "abccccdd"

字符计数：
  a: 1 → 1/2*2 = 0
  b: 1 → 1/2*2 = 0
  c: 4 → 4/2*2 = 4
  d: 2 → 2/2*2 = 2

偶数部分总和：0 + 0 + 4 + 2 = 6
存在奇数次数的字符（a 和 b），中间可以放 1 个：+1

最长回文串长度 = 6 + 1 = 7
```

**时间复杂度：** O(n)

**空间复杂度：** O(1)，固定 128 大小的数组

## Go 解法

```go
func longestPalindrome(s string) int {
    count := [128]int{}
    for _, v := range s {
        count[v]++
    }

    length := 0
    hasOdd := false
    for _, v := range count {
        length += v / 2 * 2
        if v % 2 == 1 {
            hasOdd = true
        }
    }

    if hasOdd {
        length++
    }
    return length
}
```

**说明：** 使用 `[128]int{}` 数组覆盖全部 ASCII 字符范围，字符码点直接作为索引。Go 中整数除法自动截断小数，`v / 2 * 2` 等价于取最大偶数部分。

## PHP 解法

```php
class Solution {

    /**
     * @param String $s
     * @return Integer
     */
    function longestPalindrome($s) {
        $count = [];
        $len = strlen($s);
        for ($i = 0; $i < $len; $i++) {
            $count[$s[$i]] = ($count[$s[$i]] ?? 0) + 1;
        }

        $length = 0;
        $hasOdd = false;
        foreach ($count as $c) {
            $length += intdiv($c, 2) * 2;
            if ($c % 2 == 1) {
                $hasOdd = true;
            }
        }

        if ($hasOdd) {
            $length++;
        }
        return $length;
    }
}
```

**说明：** PHP 中除法可能产生浮点数，需要用 `intdiv()` 进行整数除法。`$count[$s[$i]] ?? 0` 避免 PHP 8+ 的 Undefined array key 警告。

#leetcode #哈希表 #字符串 #贪心
