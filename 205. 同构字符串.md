
**题目链接**：[https://leetcode.cn/problems/isomorphic-strings/description](https://leetcode.cn/problems/isomorphic-strings/description)

---

## 题目描述

给定两个字符串 `s` 和 `t`，判断它们是否是同构的。

如果 `s` 中的字符可以按某种映射关系替换得到 `t`，那么这两个字符串是同构的。

每个出现的字符都应当映射到另一个字符，同时不改变字符的顺序。不同字符不能映射到同一个字符上，相同字符只能映射到同一个字符上，字符可以映射到自己本身。

**示例 1：**

```
输入：s = "egg", t = "add"
输出：true
解释：将 'e' 映射为 'a'，将 'g' 映射为 'd'。
```

**示例 2：**

```
输入：s = "f11", t = "b23"
输出：false
解释：'1' 需要同时映射到 '2' 和 '3'。
```

**示例 3：**

```
输入：s = "paper", t = "title"
输出：true
```

**提示：**

- `1 <= s.length <= 5 * 10^4`
- `t.length == s.length`
- `s` 和 `t` 由任意有效的 ASCII 字符组成


## 解题思路

核心要点：**双向映射**。需要两个 map 分别记录 s→t 和 t→s 的映射关系。

- `s2t`：保证同一个 s 字符映射到同一个 t 字符（规则1）
- `t2s`：保证不同的 s 字符不能映射到同一个 t 字符（规则2）

遍历每个位置：
- 如果 `s[i]` 已经有映射，检查是否映射到 `t[i]`，不一致则返回 false
- 如果 `s[i]` 没有映射，但 `t[i]` 已经被其他字符映射了，也返回 false
- 否则建立双向映射

**为什么需要两个 map？** 例如 `s = "ab"`, `t = "aa"`，只检查 s→t 时 `a→a` 和 `b→a` 都没问题，但两个不同字符都映射到了 `a`，违反了规则2。t→s 的 map 就是为了防止这种情况。

**时间复杂度：** O(n)

**空间复杂度：** O(k)，k 为字符集大小

## Go 解法

```go
func isIsomorphic(s string, t string) bool {
    s2t := make(map[rune]rune)
    t2s := make(map[rune]rune)
    for i, ch := range s {
        if mapped, ok := s2t[ch]; ok {
            if mapped != rune(t[i]) {
                return false
            }
        } else {
            if _, ok := t2s[rune(t[i])]; ok {
                return false
            }
            s2t[ch] = rune(t[i])
            t2s[rune(t[i])] = ch
        }
    }
    return true
}
```

## PHP 解法

```php
class Solution {

    /**
     * @param String $s
     * @param String $t
     * @return Boolean
     */
    function isIsomorphic($s, $t) {
        $s2t = [];
        $t2s = [];

        for ($i = 0; $i < strlen($s); $i++) {
            $ss = $s[$i];
            $ts = $t[$i];

            if (isset($s2t[$ss])) {
                if ($s2t[$ss] != $ts) {
                    return false;
                }
            } else {
                if (isset($t2s[$ts])) {
                    return false;
                }
                $s2t[$ss] = $ts;
                $t2s[$ts] = $ss;
            }
        }

        return true;
    }
}
```

#leetcode #哈希表 #字符串
