
**题目链接**：[https://leetcode.cn/problems/reverse-linked-list/description](https://leetcode.cn/problems/reverse-linked-list/description)

---

## 题目描述

给你单链表的头节点 `head` ，请你反转链表，并返回反转后的链表。

**示例 1：**

![](https://assets.leetcode.com/uploads/2021/02/19/rev1ex1.jpg)

**输入：** head = [1,2,3,4,5]
**输出：**[5,4,3,2,1]

**示例 2：**

![](https://assets.leetcode.com/uploads/2021/02/19/rev1ex2.jpg)

**输入：** head = [1,2]
**输出：**[2,1]

**示例 3：**

**输入：** head = []
**输出：**[]

**提示：**

- 链表中节点的数目范围是 `[0, 5000]`
- `-5000 <= Node.val <= 5000`

**进阶：**链表可以选用迭代或递归方式完成反转。你能否用两种方法解决这道题？

## 解题思路 ：

### 2.1 迭代（双指针）

构造一个pre节点，pre节点和cur节点同步向后循环，将节点不断指向pre节点，从而实现反转。

### 2.2 递归

先通过递归将链表元素反转排序，然后从最底层（尾部节点）开始局部反转节点。


## 代码实现

## 迭代

### PHP 解法

```php
/**
 * Definition for a singly-linked list.
 * class ListNode {
 *     public $val = 0;
 *     public $next = null;
 *     function __construct($val = 0, $next = null) {
 *         $this->val = $val;
 *         $this->next = $next;
 *     }
 * }
 */
class Solution {

    /**
     * @param ListNode $head
     * @return ListNode
     */
    function reverseList($head) {
        $pre = null;
        $cur = $head;

        while($cur){
            $tmp = $cur->next;
            $cur->next = $pre;
            $pre = $cur;
            $cur = $tmp;
        }
        return $pre;
        
    }
}
```

### Go 解法

```go
/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */
func reverseList(head *ListNode) *ListNode {
    var pre *ListNode
    cur := head

    for(cur != nil){
        tmp := cur.Next
        cur.Next = pre
        pre = cur
        cur = tmp
    }

    return pre
}
```

## 递归

### PHP 解法

```php
/**
 * Definition for a singly-linked list.
 * class ListNode {
 *     public $val = 0;
 *     public $next = null;
 *     function __construct($val = 0, $next = null) {
 *         $this->val = $val;
 *         $this->next = $next;
 *     }
 * }
 */
class Solution {

    /**
     * @param ListNode $head
     * @return ListNode
     */
    function reverseList($head) {
        if($head == null || $head->next == null){
            return $head;
        }
        $res = $this->reverseList($head->next);
        $head->next->next = $head;
        $head->next = null;

        return $res;
        
    }
}
```

### Go 解法

```go
/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */
func reverseList(head *ListNode) *ListNode {
    if head == nil || head.Next == nil {
        return head
    }

    res := reverseList(head.Next)

    head.Next.Next = head
    head.Next = nil

    return res 
    
}
```
## 关键点

### 4.1 迭代

引入一个pre节点

#### 4.2 递归

1. 最深层递归返回的是原链表的尾节点，它将成为整个反转后链表的新头节点。
    - 这个节点在递归过程中**始终保持不变**，后续所有层都直接返回它。

2. 回溯过程中逐层完成“局部反转”：
    - 每一层将当前节点 `head` 接到已反转子链表的**末尾**（即 `head->next->next = head`）。
    - 同时必须**断开原前向链接**（`head->next = null`），防止形成环。

3. 递归函数返回的始终是同一个头节点（即原尾节点）**，但它所代表的链表在回溯过程中**逐步扩展，每返回一层，就在其尾部追加一个新节点。

4. 基线条件必须同时处理空链表和单节点链表

5. 递归的本质是“先递进到底，再从后往前改指针”
    - 不是在递归调用前修改结构，而是在**回溯阶段**（即函数返回过程中）修改 `next` 指针，实现自底向上的反转。

## 复杂度分析

### 5.1 迭代

- 时间复杂度 O(N) ： N是原链表长度，需要遍历一次原链表。
- 空间复杂度 O(1) ： 变量 `pre` 和 `cur` 使用常数大小额外空间。

### 5.2 递归

- 时间复杂度 O(N) ： N是原链表长度，需要遍历一次原链表。
- 空间复杂度 O(N) ： 虽然每层递归使用是确定个数的常量，也就是O(1)的复杂度，但是递归N次，所以总的空间复杂度就是O(1) * N = O(N)。

#leetcode #链表 #双指针 #递归
