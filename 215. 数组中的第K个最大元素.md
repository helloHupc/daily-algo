
**题目链接**：[https://leetcode.cn/problems/kth-largest-element-in-an-array/description](https://leetcode.cn/problems/kth-largest-element-in-an-array/description)

---
## 题目描述

给定整数数组 `nums` 和整数 `k`，请返回数组中第 `k` 个最大的元素。

请注意，你需要找的是数组排序后的第 `k` 个最大的元素，而不是第 `k` 个不同的元素。

你必须设计并实现时间复杂度为 **O(n)** 的算法解决此问题。

**示例 1：**

```
输入：[3,2,1,5,6,4], k = 2
输出：5
```

**示例 2：**

```
输入：[3,2,3,1,2,4,5,5,6], k = 4
输出：4
```

**提示：**

- `1 <= k <= nums.length <= 10^5`
- `-10^4 <= nums[i] <= 10^4`

## 解题思路

### 核心思想：三路分区 + 递归（空间换时间）

普通的 Quickselect 为什么可能超时？两种最坏情况：

1. **近乎有序的数组** + 固定选最后一个当 pivot → 每轮只排除 1 个元素
2. **大量重复元素**（如全是 1）→ 等于 pivot 的元素全放一边，同样每轮只排除 1 个

**三路分区**把数组分成三块，一次性解决两个问题：

```
[ > pivot ]  [ == pivot ]  [ < pivot ]
     ↑             ↑             ↑
   大数区        相等区        小数区
```

**为什么这样就好了？**

| 情况 | 普通分区（≥ / <） | 三路分区（> / == / <） |
|------|------------------|---------------------|
| 大量重复 | 全堆一边，O(n²) | 相等区直接命中，O(n) ✅ |
| 近乎有序 | 随机 pivot 解决 | 随机 pivot 同样解决 ✅ |

**算法流程（Krahets 版）：**

1. 随机选一个 pivot
2. 遍历数组，把元素分到三个新数组：
   - `big`（> pivot）
   - `equal`（== pivot）
   - `small`（< pivot）
3. 比较 k 落在哪个区域：
   - `k <= len(big)` → 去 `big` 递归找（k 不变）
   - `k > len(big) + len(equal)` → 去 `small` 递归找（k 减去 `big` 和 `equal` 的总数）
   - 否则 → **返回 pivot** ✅

> 这种写法不追求 in-place 操作，而是用三个临时数组（空间换时间），代码极其简洁直观。对于 `10^5` 级别的输入，额外空间完全可接受。

### 复杂度分析

- **时间复杂度**：O(n) — 平均，最坏 O(n²)（概率极低）
- **空间复杂度**：O(n) — 递归调用栈 + 临时数组

## PHP 解法

```php
class Solution {

    /**
     * @param Integer[] $nums
     * @param Integer $k
     * @return Integer
     */
    function findKthLargest($nums, $k) {
        // 随机选 pivot
        $pivot = $nums[rand(0, count($nums) - 1)];

        $big = $equal = $small = [];
        foreach ($nums as $num) {
            if ($num > $pivot) {
                $big[] = $num;
            } elseif ($num < $pivot) {
                $small[] = $num;
            } else {
                $equal[] = $num;
            }
        }

        $bigCount = count($big);
        $equalCount = count($equal);

        if ($k <= $bigCount) {
            // 第 k 大在 big 中
            return $this->findKthLargest($big, $k);
        }
        if ($k > $bigCount + $equalCount) {
            // 第 k 大在 small 中（减去 big 和 equal 的数量）
            return $this->findKthLargest($small, $k - $bigCount - $equalCount);
        }
        // 第 k 大在 equal 中
        return $pivot;
    }
}
```

**说明：**
- 不追求 in-place 操作，用三个临时数组（`big`、`equal`、`small`）存放分区结果
- 递归只在需要的那一侧进行，另一侧直接丢弃
- 大量重复元素时，`equal` 区直接命中，不会退化到 O(n²)

## Go 解法

```go
import "math/rand"

func findKthLargest(nums []int, k int) int {
    pivot := nums[rand.Intn(len(nums))]

    big, equal, small := []int{}, []int{}, []int{}
    for _, num := range nums {
        if num > pivot {
            big = append(big, num)
        } else if num < pivot {
            small = append(small, num)
        } else {
            equal = append(equal, num)
        }
    }

    if k <= len(big) {
        return findKthLargest(big, k)
    }
    if k > len(big)+len(equal) {
        return findKthLargest(small, k-len(big)-len(equal))
    }
    return pivot
}
```

**说明：**
- 代码和 PHP 版几乎一模一样，Krahets 这个写法语言差异极小
- 用三个 slice 做分区，递归只进入需要的那一侧
- 大量重复元素时 equal 区直接命中，O(n) 不会退化

## Go 与 PHP 差异总结

本题采用 Krahets 的简洁写法，不涉及 in-place 分区，因此两种语言的代码几乎一模一样。以下是少数细微差别：

| 对比项 | PHP | Go |
|--------|-----|-----|
| **新建切片** | `$big = $equal = $small = []` | `big, equal, small := []int{}, []int{}, []int{}` |
| **追加元素** | `$big[] = $num` | `big = append(big, num)` |
| **获取长度** | `count($big)` | `len(big)` |
| **随机数** | `rand(0, count($nums)-1)` | `rand.Intn(len(nums))` |
| **递归调用** | `$this->findKthLargest($big, $k)` | `findKthLargest(big, k)` |
| **函数参数** | 数组默认传值（副本） | 切片传递底层指针（但新建切片互不影响） |

### 关键差异：Krahets 写法的好处

- **没有 in-place 交换逻辑** → 不需要 `&$nums` 引用传参、不需要指针操作
- **递归路径清晰** → 每次调用创建全新数组，不存在索引越界问题
- **两语言代码几乎能直接对应** → 从 PHP 翻译到 Go 几乎只是语法替换

## 扩展知识

### 解法补充：最小堆（O(n log k) — 不满足本题 O(n) 要求，但值得了解）

维护一个大小为 k 的最小堆，遍历数组：
- 堆大小 < k → 直接入堆
- 堆大小 == k → 如果当前元素 > 堆顶，弹出堆顶再入堆

最终堆顶就是第 k 大的元素。

```php
// PHP 没有内置堆，需要用 SplMinHeap
class Solution {
    function findKthLargest($nums, $k) {
        $heap = new SplMinHeap();
        foreach ($nums as $num) {
            $heap->insert($num);
            if ($heap->count() > $k) {
                $heap->extract();
            }
        }
        return $heap->top();
    }
}
```

```go
// Go 需要自己实现 heap.Interface，或者用 container/heap
// 最简方式还是用 Quickselect
```

#leetcode #数组 #TOP_K #快速选择 #分治
