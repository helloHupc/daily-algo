
**题目链接**：[https://leetcode.cn/problems/binary-search/description](https://leetcode.cn/problems/binary-search/description)

---
## 题目描述

给定一个 `n` 个元素有序的（升序）整型数组 `nums` 和一个目标值 `target`，写一个函数搜索 `nums` 中的 `target`，如果目标值存在返回下标，否则返回 `-1`。

**示例 1：**

```
输入：nums = [-1,0,3,5,9,12], target = 9
输出：4
解释：9 出现在 nums 中并且下标为 4
```

**示例 2：**

```
输入：nums = [-1,0,3,5,9,12], target = 2
输出：-1
解释：2 不存在 nums 中因此返回 -1
```

**提示：**

- 你可以假设 `nums` 中的所有元素是不重复的
- `n` 将在 `[1, 10000]` 之间
- `nums` 的每个元素都将在 `[-9999, 9999]` 之间

## 解题思路

### 二分查找（Binary Search）

这是最经典的二分查找模板，核心思想是**每次将搜索范围缩小一半**。

**前提条件：** 数组必须是有序的（本题已排序 + 无重复元素，完美适配）。

**算法流程：**

1. 定义左右指针 `left = 0`，`right = n - 1`
2. 循环直到 `left > right`：
   - 计算中间位置 `mid = left + (right - left) / 2`（防溢出写法）
   - 如果 `nums[mid] == target` → 找到，返回 `mid`
   - 如果 `nums[mid] < target` → target 在右半部分，`left = mid + 1`
   - 如果 `nums[mid] > target` → target 在左半部分，`right = mid - 1`
3. 循环结束仍未找到 → 返回 `-1`

```
搜索过程示例：nums = [-1,0,3,5,9,12], target = 9

初始：left=0, right=5, mid=(0+5)/2=2, nums[2]=3 < 9 → left=mid+1=3
第2轮：left=3, right=5, mid=(3+5)/2=4, nums[4]=9 == 9 → 返回 4 ✓
```

**为什么用 `mid = left + (right - left) / 2` 而不是 `(left + right) / 2`？**

因为当 `left + right` 超过 int 范围时会溢出，用减法代替加法可以避免这个问题。Go 中 `int` 在 64 位系统上是 64 位，本题数据范围较小看不出区别，但这是一个良好的编程习惯。

**时间复杂度：** O(log n) — 每次将搜索范围缩小一半

**空间复杂度：** O(1) — 只用了几个变量

## PHP 解法

```php
class Solution {

    /**
     * @param Integer[] $nums
     * @param Integer $target
     * @return Integer
     */
    function search($nums, $target) {
        $left = 0;
        $right = count($nums) - 1;

        while ($left <= $right) {
            $mid = $left + intval(($right - $left) / 2);

            if ($nums[$mid] == $target) {
                return $mid;
            } elseif ($nums[$mid] < $target) {
                $left = $mid + 1;
            } else {
                $right = $mid - 1;
            }
        }

        return -1;
    }
}
```

**说明：**
- PHP 是弱类型语言，但除法 `/` 默认返回 `float`，所以需要 `intval()` 取整，或者用 `>> 1` 位运算替代
- 也可以用 `$mid = $left + (($right - $left) >> 1);` 来避免浮点数问题
- 循环条件 `$left <= $right`，注意这里是有等号的，因为当 left == right 时，mid 指向的元素也需要判断

## Go 解法

```go
func search(nums []int, target int) int {
    left, right := 0, len(nums)-1

    for left <= right {
        mid := left + (right-left)/2

        if nums[mid] == target {
            return mid
        } else if nums[mid] < target {
            left = mid + 1
        } else {
            right = mid - 1
        }
    }

    return -1
}
```

**说明：**
- Go 是强类型语言，`len(nums)` 返回 `int`，所以 `right` 的类型自动推导为 `int`
- `left + (right-left)/2` 中的除法是**整数除法**（两个 `int` 相除），结果自动截断，不需要像 PHP 那样 `intval()`
- Go 的 `for` 是唯一的循环关键字，语法类似 C，没有 PHP 的 `while` 关键字

## Go 与 PHP 差异总结

| 对比项 | PHP | Go |
|--------|-----|----|
| **类型系统** | 弱类型，变量不需要声明类型 | 强类型，变量类型在编译期确定 |
| **除法运算** | `/` 返回 float，需 `intval()` 取整 | 整数除法直接截断，无需额外处理 |
| **数组长度** | `count($nums)` — 函数调用 | `len(nums)` — 内置函数 |
| **变量声明** | `$left = 0` — 直接赋值 | `left := 0` — 短变量声明 (`:=`) |
| **循环语法** | `while (条件) { }` | `for 条件 { }` — Go 只有 `for` |
| **返回 nil** | `return -1` — 返回整数 | `return -1` — 相同 |
| **else if** | `elseif` 或 `else if` 均可 | 必须写成 `else if`（中间有空格） |

### 经典踩坑点

1. **循环条件要不要等号？** `left <= right` 还是 `left < right`？
   - 当使用 `right = n-1` 初始化时（左闭右闭区间），必须用 `<=`，否则会漏掉 `left == right` 时那一个元素的判断
   - 对应地，更新时是 `left = mid + 1` 和 `right = mid - 1`

2. **Go 的整数除法行为：** Go 中两个 `int` 相除结果直接截断小数部分（向零取整），而 PHP 需要用 `intval()` 显式转换或使用 `>> 1` 位运算

3. **数组越界：** 当 `nums` 为空时，`len(nums)-1` 会得到 `-1`，导致 `left(0) > right(-1)`，循环不会执行，直接返回 `-1`，恰好正确。但题目约束 `n >= 1`，所以一般不会出现这个情况。

## 扩展知识：二分查找的三种区间写法

二分查找有多种区间定义写法，这里列出常见两种：

### 写法一：左闭右闭 `[left, right]`（本题使用）

```go
left, right := 0, len(nums)-1
for left <= right {           // <= 是因为 left == right 时区间有效
    mid := left + (right-left)/2
    if nums[mid] == target {
        return mid
    } else if nums[mid] < target {
        left = mid + 1         // target 在右半，mid 已排除
    } else {
        right = mid - 1        // target 在左半，mid 已排除
    }
}
```

### 写法二：左闭右开 `[left, right)`

```go
left, right := 0, len(nums)    // right 取 len(nums) 而不是 len(nums)-1
for left < right {             // < 是因为 left == right 时区间为空
    mid := left + (right-left)/2
    if nums[mid] == target {
        return mid
    } else if nums[mid] < target {
        left = mid + 1         // target 在右半，mid 已排除
    } else {
        right = mid            // target 在左半，right 开区间不包含 mid
    }
}
```

**建议：** 初学阶段先掌握一种写法（推荐左闭右闭 `<=`），用熟了再理解其他变体。关键是**区间定义要一致**——区间定义决定了循环条件和指针更新的写法。

#leetcode #数组 #二分查找 #binary-search
