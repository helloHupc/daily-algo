
**题目链接**：[https://leetcode.cn/problems/find-the-duplicate-number/description](https://leetcode.cn/problems/find-the-duplicate-number/description)

---
## 题目描述

给定一个包含 `n + 1` 个整数的数组 `nums` ，其数字都在 `[1, n]` 范围内（包括 `1` 和 `n`），可知至少存在一个重复的整数。

假设 `nums` 只有 **一个重复的整数** ，返回 **这个重复的数** 。

你设计的解决方案必须 **不修改** 数组 `nums` 且只用常量级 `O(1)` 的额外空间。

**示例 1：**

```
输入：nums = [1,3,4,2,2]
输出：2
```

**示例 2：**

```
输入：nums = [3,1,3,4,2]
输出：3
```

**示例 3：**

```
输入：nums = [3,3,3,3,3]
输出：3
```

**提示：**

- `1 <= n <= 10^5`
- `nums.length == n + 1`
- `1 <= nums[i] <= n`
- `nums` 中 **只有一个整数** 出现 **两次或多次** ，其余整数均只出现 **一次**

**进阶：**
- 如何证明 `nums` 中至少存在一个重复的数字？
- 你可以设计一个线性级时间复杂度 `O(n)` 的解决方案吗？

## 解题思路

### 核心思想：将数组视为链表，用 Floyd 判环算法

这道题有几个看似矛盾的限制条件：
- 不能修改数组（不能用排序、原地标记）
- 只能用 O(1) 额外空间（不能用哈希表）
- 要在 O(n) 时间内解决

关键**奇技淫巧**：把数组当成一个**链表**来处理。

#### 如何将数组映射成链表？

数组有 `n+1` 个元素，值都在 `[1, n]` 范围内。我们可以把**值**看作指向下一个数组元素的**指针**：

```
从下标 i 出发，下一个元素的下标 = nums[i]
```

用这个规则遍历数组，因为 `nums[0]` 一定在 `[1, n]` 范围内，我们就从下标 `0` 开始走：

```
0 → nums[0] → nums[nums[0]] → nums[nums[nums[0]]] → ...
```

因为有 `n+1` 个元素，值却是 `1 ~ n`（只有 n 种可能），根据鸽巢原理，**至少有一个值出现两次**。在链表视角下，这意味着有两个不同的下标指向了同一个下标，于是形成了**环**。

比如 `nums = [1,3,4,2,2]`：

```
下标:  0 → 1 → 3 → 2 → 4 → 2 → 4 → 2 → ...  （进入环 2↔4）
值:   1   3   2   4   2
```

重复的数字 `2`，正好就是**环的入口**。

#### Floyd 判环算法（快慢指针）

1. **阶段一：找相遇点**
   - 慢指针 `slow` 每次走一步：`slow = nums[slow]`
   - 快指针 `fast` 每次走两步：`fast = nums[nums[fast]]`
   - 当 `slow == fast` 时相遇（环内相遇）

2. **阶段二：找环入口**
   - 将 `slow` 重置为起点 `0`
   - `slow` 和 `fast` 都每次走一步
   - 再次相遇的点就是环的入口，即重复的数字

#### 复杂度分析

- **时间复杂度**：O(n)，Floyd 算法线性时间
- **空间复杂度**：O(1)，只用了两个指针变量

### 为什么这个算法是正确的？

**为什么把数组当链表会有环？**

因为数组有 `n+1` 个元素，每个元素的值在 `[1, n]` 内，所以每个值都可以作为数组下标。从下标 `0` 开始，根据 `nums[i]` 跳转到下一个下标，形成一条路径。由于值的范围只有 `n` 种，却有 `n+1` 个位置，所以一定存在重复值，导致两个不同的位置跳到同一个下标，路径就产生了环。

**为什么环的入口就是重复的数字？**

重复的数字意味着有**两个不同的下标** `i` 和 `j` 的 `nums[i] == nums[j]`。在链表视角中，这两个下标都指向同一个节点，因此这个节点就是环的入口。Floyd 算法找到的环入口就是它。

## PHP 解法

```php
class Solution
{
    /**
     * @param Integer[] $nums
     * @return Integer
     */
    function findDuplicate(array $nums): int
    {
        // 阶段一：找到快慢指针的相遇点
        $slow = $nums[0];
        $fast = $nums[$nums[0]];

        while ($slow !== $fast) {
            $slow = $nums[$slow];
            $fast = $nums[$nums[$fast]];
        }

        // 阶段二：找环的入口
        $slow = 0;
        while ($slow !== $fast) {
            $slow = $nums[$slow];
            $fast = $nums[$fast];
        }

        return $slow;
    }
}
```

## Go 解法

```go
func findDuplicate(nums []int) int {
    // 阶段一：找到快慢指针的相遇点
    slow := nums[0]
    fast := nums[nums[0]]

    for slow != fast {
        slow = nums[slow]
        fast = nums[nums[fast]]
    }

    // 阶段二：找环的入口
    slow = 0
    for slow != fast {
        slow = nums[slow]
        fast = nums[fast]
    }

    return slow
}
```

## Go 与 PHP 差异总结

| 对比项 | PHP | Go |
|--------|-----|-----|
| **类型声明** | `function findDuplicate(array $nums): int` | `func findDuplicate(nums []int) int` |
| **变量赋值** | `$slow = $nums[0]` | `slow := nums[0]` |
| **不等判断** | `$slow !== $fast`（`!=` 也可） | `slow != fast`（Go 无 `!==`） |
| **数组访问** | `$nums[$slow]` | `nums[slow]` |
| **返回** | `return $slow;` | `return slow` |

### 关键差异点

1. **变量声明**：PHP 用 `$` 前缀，Go 用 `:=` 声明并赋值。Go 的 `:=` 是声明+初始化，后续修改用 `=` 即可。注意 Go 中不能对一个已声明过的变量再用 `:=`。

2. **数组 vs 切片**：PHP 的 `array` 是哈希表，支持动态增删；Go 的切片是连续内存块。本题只用读操作，两者差异不大，但 Go 的切片访问性能远高于 PHP 的数组。

3. **快慢指针的写法**：PHP 需要反复使用 `$nums[]` 访问；Go 相同，但少了 `$` 符号，更加简洁。

## 扩展知识

### 其他解法对比

| 解法 | 时间复杂度 | 空间复杂度 | 是否能修改数组 |
|------|-----------|-----------|--------------|
| **排序后遍历** | O(n log n) | O(1) | ❌ 会修改 |
| **哈希表** | O(n) | O(n) | ✅ 不修改 |
| **二分查找（值域）** | O(n log n) | O(1) | ✅ 不修改 |
| **位运算** | O(n log n) | O(1) | ✅ 不修改 |
| **Floyd 判环** ⭐ | **O(n)** | **O(1)** | ✅ 不修改 |

### 为什么二分查找能用在值域上而不用在索引上？

常规二分查找是对有序数组的索引进行二分。这道题虽然数组无序，但**值域** `[1, n]` 是有序的。我们可以对值域二分：

- 对于 `mid`，统计数组中 `<= mid` 的个数 `count`
- 如果 `count > mid`，说明重复数在 `[1, mid]` 中
- 否则在 `[mid+1, n]` 中

这利用了**鸽巢原理**：在 `1 ~ mid` 这 `mid` 个数中，如果出现次数超过 `mid` 次，一定有重复。

### 鸽子洞原理（Pigeonhole Principle）

本题的进阶问题：如何证明 `nums` 中至少存在一个重复的数字？

> 有 `n+1` 只鸽子（数组元素）要放进 `n` 个鸽巢（值 `1 ~ n`），至少有一个鸽巢有两只以上的鸽子。

这就是鸽巢原理，也是这道题一切解法的基础。

#leetcode #数组 #双指针 #二进制 #位运算 #二分查找
