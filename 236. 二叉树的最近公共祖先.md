
**题目链接**：[https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/description](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/description)

---
## 题目描述

给定一个二叉树，找到该树中两个指定节点的最近公共祖先。

百度百科中最近公共祖先的定义为："对于有根树 T 的两个节点 p、q，最近公共祖先表示为一个节点 x，满足 x 是 p、q 的祖先且 x 的深度尽可能大（一个节点也可以是它自己的祖先）。"

**示例 1：**

```
输入：root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 1
输出：3
解释：节点 5 和节点 1 的最近公共祖先是节点 3 。
```

**示例 2：**

```
输入：root = [3,5,1,6,2,0,8,null,null,7,4], p = 5, q = 4
输出：5
解释：节点 5 和节点 4 的最近公共祖先是节点 5 。因为根据定义最近公共祖先节点可以为节点本身。
```

**示例 3：**

```
输入：root = [1,2], p = 1, q = 2
输出：1
```

**提示：**

- 树中节点数目在范围 `[2, 10^5]` 内
- `-10^9 <= Node.val <= 10^9`
- 所有 `Node.val` 互不相同
- `p != q`
- `p` 和 `q` 均存在于给定的二叉树中

## 解题思路

### 核心思想：后序遍历（自底向上查找）

最近公共祖先（LCA）问题最优雅的解法是**递归后序遍历**。

想象你是从树的最底层往上找：如果某个节点的左子树里找到了 p 或 q，右子树里也找到了另一个，那么这个节点就是 LCA。

**关键推理：**

1. 从根节点出发，递归地往下找 p 和 q
2. 如果当前节点本身就是 p 或 q，直接返回当前节点（**一个节点也可以是它自己的祖先**）
3. 分别从左右子树中查找结果：
   - **左右都找到了** → 当前节点就是 LCA（p 和 q 分别在两侧）
   - **只在左子树找到** → 返回左子树的查找结果（LCA 在左边）
   - **只在右子树找到** → 返回右子树的查找结果（LCA 在右边）
   - **都没找到** → 返回 nil

**为什么这是对的？**

因为 p 和 q 的位置关系只有两种可能：
- **一个在左子树，一个在右子树** → 当前节点就是最近的交汇点
- **一个在另一个的子树里** → 先找到的那个（更浅的）就是 LCA

### 复杂度分析

- **时间复杂度**：O(n) — 每个节点只访问一次
- **空间复杂度**：O(h) — 递归栈的深度，h 为树的高度。最坏情况（链状树）O(n)，平均 O(log n)

## PHP 解法

```php
class Solution {

    /**
     * @param TreeNode $root
     * @param TreeNode $p
     * @param TreeNode $q
     * @return TreeNode
     */
    function lowestCommonAncestor($root, $p, $q) {
        // 如果当前节点为空，或等于 p 或 q，直接返回
        if ($root === null || $root === $p || $root === $q) {
            return $root;
        }

        // 递归在左右子树中查找
        $left = $this->lowestCommonAncestor($root->left, $p, $q);
        $right = $this->lowestCommonAncestor($root->right, $p, $q);

        // 如果左右都不为空，说明 p 和 q 分居两侧，当前节点就是 LCA
        if ($left !== null && $right !== null) {
            return $root;
        }

        // 否则返回非空的那一侧（两侧都为空则返回 null）
        return $left !== null ? $left : $right;
    }
}
```

**说明：**
- 代码非常简洁，本质就是后序遍历的变体——先处理左右子树，再根据结果判断当前节点
- `$root === $p` 和 `$root === $q` 用的是**引用比较**（对象比较），而不是值比较，因为节点值可能相同（虽然题目说互不相同，但用引用更严谨）
- 注意这里比较用的是 `===`（严格等于），PHP 中对象比较用 `===` 比较的是引用，用 `==` 比较的是属性

## Go 解法

```go
/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */

func lowestCommonAncestor(root, p, q *TreeNode) *TreeNode {
    // 如果当前节点为空，或等于 p 或 q，直接返回
    if root == nil || root == p || root == q {
        return root
    }

    // 递归在左右子树中查找
    left := lowestCommonAncestor(root.Left, p, q)
    right := lowestCommonAncestor(root.Right, p, q)

    // 如果左右都不为空，说明 p 和 q 分居两侧，当前节点就是 LCA
    if left != nil && right != nil {
        return root
    }

    // 否则返回非空的那一侧（两侧都为空则返回 nil）
    if left != nil {
        return left
    }
    return right
}
```

**说明：**
- Go 的 `==` 用于指针比较，天然就是引用比较（比较的是指针地址），无需区分 `==` 和 `===`
- 递归逻辑与 PHP 完全一致，但因为强类型，返回值类型明确是 `*TreeNode`

## Go 与 PHP 差异总结

| 对比项 | PHP | Go |
|--------|-----|-----|
| **相等比较** | `===` 比较对象引用，`==` 比较对象属性 | `==` 直接比较指针地址（就是引用比较） |
| **递归函数调用** | `$this->lowestCommonAncestor(...)` | `lowestCommonAncestor(...)`（包级函数） |
| **null/nil** | `null` | `nil` |
| **三元表达式** | `$left !== null ? $left : $right` | 没有三元运算符，需要用 `if-else` |
| **返回值类型** | 弱类型，注释标注 `@return TreeNode` | 强类型，签名明确 `*TreeNode` |
| **函数命名风格** | `camelCase` | `camelCase`（两者一致） |

### 关于 `===` vs `==` 的重要说明

这是一个面试中常见的考点：

```php
$a = new TreeNode(5);
$b = new TreeNode(5);

$a === $b; // false — 不同的对象实例
$a == $b;  // true  — 属性相同

// 但在 LCA 问题中，p 和 q 是树中已有的节点引用
// 所以 $root === $p 比较的是"是不是同一个对象实例"
// 这正是我们需要的！
```

在 LCA 问题中，p 和 q 是传入的**引用**，我们需要判断当前节点是不是**同一个对象**，所以必须用 `===`。

## 扩展知识

### 父指针法（另一种思路）

如果题目允许修改树的结构（或事先存储父指针），可以用**哈希表**记录每个节点的父节点：

```go
func lowestCommonAncestor(root, p, q *TreeNode) *TreeNode {
    parent := make(map[*TreeNode]*TreeNode)
    visited := make(map[*TreeNode]bool)

    // DFS 记录每个节点的父节点
    var dfs func(node *TreeNode)
    dfs = func(node *TreeNode) {
        if node == nil {
            return
        }
        if node.Left != nil {
            parent[node.Left] = node
            dfs(node.Left)
        }
        if node.Right != nil {
            parent[node.Right] = node
            dfs(node.Right)
        }
    }
    dfs(root)

    // 从 p 向上标记路径
    for p != nil {
        visited[p] = true
        p = parent[p]
    }

    // 从 q 向上找到第一个被标记的节点
    for q != nil {
        if visited[q] {
            return q
        }
        q = parent[q]
    }

    return nil
}
```

这种方法时间 O(n)、空间 O(n)，适合需要多次查询的场景。但本题是单次查询，**递归后序遍历更优**。

### 兄弟题目关联

- **235. 二叉搜索树的最近公共祖先** — 本题的简化版，可以利用 BST 的性质（左小右大）优化到 O(h)
- **236. 二叉树的最近公共祖先** — 本题，通用解法

#leetcode #二叉树 #LCA #后序遍历 #递归 #分治
