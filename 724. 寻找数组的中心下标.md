
**题目链接**：[https://leetcode.cn/problems/find-pivot-index/description](https://leetcode.cn/problems/find-pivot-index/description)

---
## 题目描述

给你一个整数数组 `nums` ，请计算数组的 **中心下标**。

数组 **中心下标** 是数组的一个下标，其左侧所有元素相加的和等于右侧所有元素相加的和。

如果中心下标位于数组最左端，那么左侧数之和视为 `0` ，因为在下标的左侧不存在元素。这一点对于中心下标位于数组最右端同样适用。

如果数组有多个中心下标，应该返回 **最靠近左边** 的那一个。如果数组不存在中心下标，返回 `-1`。

**示例 1：**

```
输入：nums = [1, 7, 3, 6, 5, 6]
输出：3
解释：
中心下标是 3 。
左侧数之和 sum = nums[0] + nums[1] + nums[2] = 1 + 7 + 3 = 11 ，
右侧数之和 sum = nums[4] + nums[5] = 5 + 6 = 11 ，二者相等。
```

**示例 2：**

```
输入：nums = [1, 2, 3]
输出：-1
解释：
数组中不存在满足此条件的中心下标。
```

**示例 3：**

```
输入：nums = [2, 1, -1]
输出：0
解释：
中心下标是 0 。
左侧数之和 sum = 0 ，（下标 0 左侧不存在元素），
右侧数之和 sum = nums[1] + nums[2] = 1 + -1 = 0 。
```

**提示：**

- `1 <= nums.length <= 10^4`
- `-1000 <= nums[i] <= 1000`

## 解题思路

### 核心思想：前缀和

中心下标的特点是：**左侧元素之和 = 右侧元素之和**。

我们可以这样推导：
- 设数组总和为 `total`
- 当遍历到下标 `i` 时，左侧元素之和为 `leftSum`
- 右侧元素之和 = `total - leftSum - nums[i]`

满足条件时：
```
leftSum = total - leftSum - nums[i]
=> 2 * leftSum + nums[i] = total
```

解题步骤：
1. 先遍历一遍数组，计算总和 `total`
2. 再次遍历数组，维护一个 `leftSum` 变量
3. 对于每个下标 `i`，检查 `leftSum == total - leftSum - nums[i]`
4. 如果成立，直接返回 `i`（因为从左往右遍历，遇到的第一个就是最左边的）
5. 否则将 `nums[i]` 累加到 `leftSum`，继续遍历
6. 遍历结束仍未找到，返回 `-1`

### 复杂度分析

- **时间复杂度**：O(n)，遍历数组两次（一次求总和，一次查找），n 为数组长度
- **空间复杂度**：O(1)，只用了几个额外变量

## PHP 解法

```php
class Solution
{
    /**
     * @param Integer[] $nums
     * @return Integer
     */
    function pivotIndex(array $nums): int
    {
        // 1. 计算数组总和
        $total = array_sum($nums);

        // 2. 遍历查找中心下标
        $leftSum = 0;
        for ($i = 0; $i < count($nums); $i++) {
            // 检查当前下标是否为中心下标
            if ($leftSum === $total - $leftSum - $nums[$i]) {
                return $i;
            }
            // 累加左侧和，为下一个下标做准备
            $leftSum += $nums[$i];
        }

        return -1;
    }
}
```

## Go 解法

```go
func pivotIndex(nums []int) int {
    // 1. 计算数组总和
    total := 0
    for _, num := range nums {
        total += num
    }

    // 2. 遍历查找中心下标
    leftSum := 0
    for i, num := range nums {
        // 检查当前下标是否为中心下标
        if leftSum == total - leftSum - num {
            return i
        }
        // 累加左侧和，为下一个下标做准备
        leftSum += num
    }

    return -1
}
```

## Go 与 PHP 差异总结

| 对比项 | PHP | Go |
|--------|-----|-----|
| **类型系统** | 弱类型，`array` 元素可以是混合类型 | 强类型，切片元素类型必须一致 |
| **数组求和方法** | 内置 `array_sum($nums)`，一行搞定 | 需要手动 `for range` 累加 |
| **循环遍历** | `for ($i = 0; $i < count($nums); $i++)` 或 `foreach` | `for i, num := range nums` 更简洁 |
| **函数返回值** | 用 `: int` 声明返回类型 | 返回值类型写在参数列表后面 |
| **变量声明** | 运行时动态声明 | 用 `:=` 或 `var` 声明，类型推断 |
| **相等判断** | `===`（严格比较） | `==`（Go 中 `==` 对基本类型就是严格比较） |
| **函数命名** | 驼峰 `pivotIndex` | 驼峰 `pivotIndex`，首字母大小写决定可见性 |

### 关键差异点

1. **数组求和**：PHP 有 `array_sum()` 内置函数可以直接计算数组总和；Go 没有现成的 `sum` 函数，需要手动遍历累加。虽然多写几行，但 Go 这种显式方式让意图更清晰，也避免了 PHP `array_sum()` 在元素类型不一致时可能产生的隐式转换问题。

2. **`count()` 与 `len()`**：PHP 用 `count($nums)` 获取数组长度，Go 用 `len(nums)`。Go 的 `len()` 是内置函数而不是方法，对切片、数组、字符串、map 等都适用。

3. **循环风格的差异**：PHP 既可以用索引循环，也可以用 `foreach` 遍历值；Go 的 `for range` 同时给出索引和值，写法更紧凑。注意 Go 中 `for range` 的循环变量是复用的（`num` 在每次迭代中是同一个内存地址），但在本例中只取值使用，不会踩坑。

4. **返回值的处理**：如果在遍历过程中没找到，循环结束后 `return -1` 是合理的兜底。PHP 中函数签名可以声明 `: int` 确保返回值类型，Go 则是在函数签名末尾声明返回类型。两者都保证了返回类型安全。

## 扩展知识

### 为什么「从左往右遍历」就能保证返回最左边的中心下标？

因为题目要求 "如果数组有多个中心下标，应该返回 最靠近左边的那一个"，我们在遍历时一旦找到符合条件的下标就立即返回，而遍历顺序恰好是从左到右，所以第一个遇到的必然是最左边的。

### 前缀和思想的应用场景

这道题是「前缀和」思想的经典入门题。前缀和数组 `prefix[i]` 表示原数组前 `i` 个元素的和。在这种解法中我们并没有显式地构建前缀和数组，而是用一个 `leftSum` 变量动态维护前缀和，达到了 O(1) 额外空间的效果。

前缀和常用于：
- 子数组求和问题（如 `560. 和为 K 的子数组`）
- 区间求和问题
- 差分数组配合使用

#leetcode #数组 #前缀和
