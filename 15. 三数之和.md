
**题目链接**：[https://leetcode.cn/problems/3sum/description](https://leetcode.cn/problems/3sum/description)

---

## 题目描述

给你一个整数数组 `nums`，判断是否存在三元组 `[nums[i], nums[j], nums[k]]` 满足 `i != j`、`i != k` 且 `j != k`，同时还满足 `nums[i] + nums[j] + nums[k] == 0`。请你返回所有和为 0 且不重复的三元组。

**注意：** 答案中不可以包含重复的三元组。

**示例 1：**

```
输入：nums = [-1,0,1,2,-1,-4]
输出：[[-1,-1,2],[-1,0,1]]
```

**示例 2：**

```
输入：nums = [0,1,1]
输出：[]
```

**示例 3：**

```
输入：nums = [0,0,0]
输出：[[0,0,0]]
```

**提示：**

- `3 <= nums.length <= 3000`
- `-10^5 <= nums[i] <= 10^5`

## 解题思路

### 排序 + 双指针

1. **排序**：先对数组排序，方便去重和双指针
2. **固定一个数**：遍历数组，固定 `nums[i]`，然后用双指针找另外两个数
3. **双指针**：`left = i+1`，`right = n-1`，根据三数之和调整指针
4. **去重**：跳过重复的数字，避免重复的三元组

```
nums = [-1,0,1,2,-1,-4]

排序后：[-4, -1, -1, 0, 1, 2]

固定 i=0, nums[i]=-4:
  left=1, right=5
  -4 + (-1) + 2 = -3 < 0 → left++
  ... 找不到满足条件的组合

固定 i=1, nums[i]=-1:
  left=2, right=5
  -1 + (-1) + 2 = 0 ✓ → 记录 [-1,-1,2]
  -1 + 0 + 1 = 0 ✓ → 记录 [-1,0,1]
  left >= right, 结束

固定 i=2, nums[i]=-1: 和 i=1 重复，跳过

固定 i=3, nums[i]=0:
  left=4, right=5
  0 + 1 + 2 = 3 > 0 → right--
  ... 找不到

结果：[[-1,-1,2], [-1,0,1]]
```

### 剪枝优化

```go
// 条件1：当前数 + 最小的两个后续数 > 0
// 数组已排序，后面只会更大，不可能凑出 0，直接结束整个循环
if nums[i] + nums[i+1] + nums[i+2] > 0 {
    break
}

// 条件2：当前数 + 最大的两个数 < 0
// 当前数太小，但后面还有更大的 nums[i+1]、nums[i+2] 可能凑出 0
// 所以 continue，跳到下一个 i，而不是 break
if nums[i] + nums[n-2] + nums[n-1] < 0 {
    continue
}
```

**区别：** `break` 是结束整个循环，`continue` 只跳过当前这次。条件2时 `nums[i]` 太小，但更大的 `nums[i+1]` 有可能凑出 0，所以要继续而不是结束。

**时间复杂度：** O(n²)

**空间复杂度：** O(1)（不算结果数组）

## Go 解法

```go
func threeSum(nums []int) [][]int {
    sort.Ints(nums)
    result := [][]int{}
    n := len(nums)

    for i := 0; i < n-2; i++ {
        // 去重：跳过重复的 nums[i]
        if i > 0 && nums[i] == nums[i-1] {
            continue
        }
        // 剪枝：当前数 + 最小的两个后续数 > 0，后面不可能有解
        if nums[i]+nums[i+1]+nums[i+2] > 0 {
            break
        }
        // 剪枝：当前数 + 最大的两个数 < 0，换下一个更大的数
        if nums[i]+nums[n-2]+nums[n-1] < 0 {
            continue
        }

        left, right := i+1, n-1
        for left < right {
            sum := nums[i] + nums[left] + nums[right]
            if sum == 0 {
                result = append(result, []int{nums[i], nums[left], nums[right]})
                // 去重：跳过重复的 left 和 right
                for left < right && nums[left] == nums[left+1] {
                    left++
                }
                for left < right && nums[right] == nums[right-1] {
                    right--
                }
                left++
                right--
            } else if sum < 0 {
                left++
            } else {
                right--
            }
        }
    }
    return result
}
```

## PHP 解法

```php
class Solution {

    /**
     * @param Integer[] $nums
     * @return Integer[][]
     */
    function threeSum($nums) {
        sort($nums);
        $result = [];
        $n = count($nums);

        for ($i = 0; $i < $n - 2; $i++) {
            // 去重：跳过重复的 nums[i]
            if ($i > 0 && $nums[$i] == $nums[$i-1]) {
                continue;
            }
            // 剪枝：当前数 + 最小的两个后续数 > 0，后面不可能有解
            if ($nums[$i] + $nums[$i+1] + $nums[$i+2] > 0) {
                break;
            }
            // 剪枝：当前数 + 最大的两个数 < 0，换下一个更大的数
            if ($nums[$i] + $nums[$n-2] + $nums[$n-1] < 0) {
                continue;
            }

            $left = $i + 1;
            $right = $n - 1;
            while ($left < $right) {
                $sum = $nums[$i] + $nums[$left] + $nums[$right];
                if ($sum == 0) {
                    $result[] = [$nums[$i], $nums[$left], $nums[$right]];
                    // 去重：跳过重复的 left 和 right
                    while ($left < $right && $nums[$left] == $nums[$left+1]) {
                        $left++;
                    }
                    while ($left < $right && $nums[$right] == $nums[$right-1]) {
                        $right--;
                    }
                    $left++;
                    $right--;
                } else if ($sum < 0) {
                    $left++;
                } else {
                    $right--;
                }
            }
        }
        return $result;
    }
}
```

#leetcode #数组 #双指针 #排序
