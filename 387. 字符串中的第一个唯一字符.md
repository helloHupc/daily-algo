
**题目链接**：[https://leetcode.cn/problems/first-unique-character-in-a-string/description](https://leetcode.cn/problems/first-unique-character-in-a-string/description)

---

## 题目描述

给定一个字符串 `s`，找到它的第一个不重复的字符，并返回它的索引。如果不存在，则返回 `-1`。

**示例 1：**

```
输入: s = "leetcode"
输出: 0
```

**示例 2：**

```
输入: s = "loveleetcode"
输出: 2
```

**示例 3：**

```
输入: s = "aabb"
输出: -1
```

**提示：**

- `1 <= s.length <= 10^5`
- `s` 只包含小写字母

## 解题思路

1. 第一次遍历：统计每个字符出现的次数
2. 第二次遍历：按原顺序找到第一个计数为 1 的字符，返回其索引
3. 若不存在，返回 -1

**时间复杂度：** O(n)，两次遍历

**空间复杂度：** O(1)，固定 26 大小的数组

## Go 解法

### 解法一：数组

```go
func firstUniqChar(s string) int {
    count := [26]int{}
    for _, ch := range s {
        count[ch-'a']++
    }
    for i, ch := range s {
        if count[ch-'a'] == 1 {
            return i
        }
    }
    return -1
}
```

**说明：** 利用 `ch - 'a'` 将小写字母映射到 `0~25` 的索引，用长度为 26 的数组统计字符出现次数，比 `map` 更高效。适用于字符范围已知且有限的情况。

### 解法二：map

```go
func firstUniqChar(s string) int {
    count := make(map[rune]int)
    for _, ch := range s {
        count[ch]++
    }
    for i, ch := range s {
        if count[ch] == 1 {
            return i
        }
    }
    return -1
}
```

**说明：** 使用 `map[rune]int` 统计字符出现次数，适用于任意字符范围，更通用但略慢于数组写法（哈希计算开销）。

## PHP 解法

```php
class Solution {

    /**
     * @param String $s
     * @return Integer
     */
    function firstUniqChar($s) {
        $count = [];
        for($i=0;$i<strlen($s);$i++){
            $count[$s[$i]]++;
        }

        for($i=0;$i<strlen($s);$i++){
            if ($count[$s[$i]] == 1) {
                return $i;
            }
        }

        return -1;
    }
}
```

**说明：** 使用 PHP 关联数组 `$count` 统计字符出现次数，`$s[$i]` 按字节访问字符串中的字符。

#leetcode #哈希表 #字符串
