
**题目链接**：[https://leetcode.cn/problems/first-bad-version/description](https://leetcode.cn/problems/first-bad-version/description)

---
## 题目描述

你是产品经理，目前正在带领一个团队开发新的产品。不幸的是，你的产品的最新版本没有通过质量检测。由于每个版本都是基于之前的版本开发的，所以错误的版本之后的所有版本都是错的。

假设你有 `n` 个版本 `[1, 2, ..., n]`，你想找出导致之后所有版本出错的第一个错误的版本。

你可以通过调用 `bool isBadVersion(version)` 接口来判断版本号 `version` 是否在单元测试中出错。实现一个函数来查找第一个错误的版本。你应该尽量减少对调用 API 的次数。

**示例 1：**

```
输入：n = 5, bad = 4
输出：4
解释：
调用 isBadVersion(3) -> false
调用 isBadVersion(5) -> true
调用 isBadVersion(4) -> true
所以，4 是第一个错误的版本。
```

**示例 2：**

```
输入：n = 1, bad = 1
输出：1
```

**提示：**

- `1 <= bad <= n <= 2^31 - 1`

## 解题思路

### 二分查找左边界

题目说"错误的版本之后的所有版本都是错的"，意味着版本序列是一个**分界点**：

```
版本号： 1  2  3  4  5  6
状态：   ✓  ✓  ✓  ✗  ✗  ✗
                   ↑
             第一个错误版本
```

这本质上是在一个**有序的布尔数组**中找第一个 `true`（左边界），是二分查找的经典变体。

**与 704. 二分查找的区别：**

| 维度 | 704 标准二分 | 278 左边界二分 |
|------|------------|--------------|
| 找什么 | 精确值（某个等于 target 的元素） | 第一个满足条件的元素（左边界） |
| 找到后 | 直接返回 mid | 不能直接返回，要继续往左搜 |
| 区间缩窄 | `left = mid + 1` / `right = mid - 1` | `left = mid + 1` / `right = mid` |

**算法流程：**

1. `left = 1, right = n`（版本号从 1 开始）
2. 循环直到 `left == right`（注意这里不是 `<=`）：
   - `mid = left + (right - left) / 2`
   - 如果 `isBadVersion(mid)` 为 **false** → 说明第一个错误版本在右边，`left = mid + 1`
   - 如果 `isBadVersion(mid)` 为 **true** → 说明第一个错误版本在 mid 或左边，`right = mid`
3. 此时 `left == right`，返回 `left`（即第一个错误版本）

**为什么 right 更新为 mid 而不是 mid - 1？**

因为 `mid` 有可能是第一个错误版本，如果把 `right` 设为 `mid - 1` 就会漏掉答案。

**为什么循环条件是 left < right 而不是 left <= right？**

因为是左边界查找，当 `left == right` 时已经锁定答案，不需要再循环了（如果用 `<=` 会死循环）。

**时间复杂度：** O(log n) — 二分查找

**空间复杂度：** O(1)

## PHP 解法

```php
/* The isBadVersion API is defined in the parent class VersionControl.
      boolean isBadVersion(int version); */

class Solution extends VersionControl {
    /**
     * @param Integer $n
     * @return Integer
     */
    function firstBadVersion($n) {
        $left = 1;
        $right = $n;

        while ($left < $right) {
            $mid = $left + intval(($right - $left) / 2);

            if ($this->isBadVersion($mid)) {
                $right = $mid;       // mid 可能是答案，不能排除
            } else {
                $left = $mid + 1;    // mid 不是错误版本，排除
            }
        }

        return $left;
    }
}
```

**说明：**
- 调用 `$this->isBadVersion($mid)` 来调用 API（题目给好的父类方法）
- `intval()` 确保整数除法，也可以用 `>> 1` 位运算
- 注意循环条件 `$left < $right`（不是 `<=`），否则会死循环

## Go 解法

```go
/**
 * Forward declaration of isBadVersion API.
 * @param   version   your guess about first bad version
 * @return 	 	      true if current version is bad
 *			          false if current version is good
 * func isBadVersion(version int) bool;
 */

func firstBadVersion(n int) int {
    left, right := 1, n

    for left < right {
        mid := left + (right-left)/2

        if isBadVersion(mid) {
            right = mid        // mid 可能是答案，不能排除
        } else {
            left = mid + 1     // mid 不是错误版本，排除
        }
    }

    return left
}
```

**说明：**
- Go 的整数除法自动截断，不需要额外处理
- 调用 `isBadVersion(mid)` 时注意这是包级别的函数（由 LeetCode 提供），不需要通过对象调用
- 函数名是 `isBadVersion`（小驼峰），不是 `IsBadVersion`

## Go 与 PHP 差异总结

| 对比项 | PHP | Go |
|--------|-----|----|
| **API 调用方式** | `$this->isBadVersion($mid)` — 通过父类方法调用 | `isBadVersion(mid)` — 包级别函数直接调用 |
| **继承/接口** | 使用 `extends VersionControl` 继承父类 | 无需继承，函数直接在包中可用 |
| **类型系统** | `$n` 自动推断为 int | `n int` 显式声明参数类型 |
| **注释风格** | 标准 `/* */` 块注释 | 使用 `/** */` 或 `//` |

### 经典踩坑点

1. **循环条件用 `left < right` 而不是 `left <= right`**：
   - 左边界二分查找在 `left == right` 时已锁定答案，继续循环会死循环
   - 标准二分（704）用 `<=`，左边界二分（278）用 `<`，这是最易混淆的地方

2. **`right = mid` 而不是 `right = mid - 1`**：
   - 因为 `mid` 本身有可能是答案（第一个错误版本），不能把它排除
   - 只有在 `isBadVersion(mid) == false` 时，才能确定 `mid` 不可能是答案，可以 `left = mid + 1`

3. **Go 中 `left + (right-left)/2` 在 left 和 right 很大时仍然安全**：
   - Go 的 `int` 在 64 位系统上是 64 位，本题 `n <= 2^31-1` 不会溢出
   - 但 `(left+right)/2` 的写法在理论上是存在溢出风险的，养成用减法版本的习惯更安全

4. **版本号从 1 开始**：
   - 初始化为 `left = 1, right = n`，不是 `0` 和 `n-1`
   - 704 的数组下标从 0 开始，278 的版本号从 1 开始，注意区分

## 扩展知识：二分查找的三种模板

| 模板 | 适用场景 | 循环条件 | 指针更新 | 返回值 |
|------|---------|---------|---------|--------|
| **标准二分** | 找精确值 | `left <= right` | `left = mid + 1` / `right = mid - 1` | `mid` |
| **左边界二分** | 找第一个满足条件的元素 | `left < right` | `left = mid + 1` / `right = mid` | `left` |
| **右边界二分** | 找最后一个满足条件的元素 | `left < right` | `left = mid` / `right = mid - 1` | `left`（或 `right`） |

**对比 704 和 278 的指针更新差异：**

```
704（标准二分）：
  nums[mid] < target → left = mid + 1
  nums[mid] > target → right = mid - 1
  因为 target 不可能在 mid 位置了，直接排除

278（左边界二分）：
  isBadVersion(mid) == false → left = mid + 1  （mid 及左边都不可能是答案）
  isBadVersion(mid) == true  → right = mid      （mid 可能是答案，保留）
```

#leetcode #数组 #二分查找 #左边界 #binary-search
