
**题目链接**：[https://leetcode.cn/problems/validate-stack-sequences/description](https://leetcode.cn/problems/validate-stack-sequences/description)

---

## 题目描述

给定 `pushed` 和 `popped` 两个序列，每个序列中的 **值都不重复**，只有当它们可能是在最初空栈上进行的推入 `push` 和弹出 `pop` 操作序列的结果时，返回 `true`；否则，返回 `false`。

**示例 1：**

**输入：**
`pushed = [1,2,3,4,5], popped = [4,5,3,2,1]`
**输出：**
`true`

**解释：**
我们可以按以下顺序执行：
push(1), push(2), push(3), push(4), pop() -> 4,
push(5), pop() -> 5, pop() -> 3, pop() -> 2, pop() -> 1

**示例 2：**

**输入：**
`pushed = [1,2,3,4,5], popped = [4,3,5,1,2]`
**输出：**
`false`

**解释：**
1 不能在 2 之前弹出。

**提示：**

- `1 <= pushed.length <= 1000`
- `0 <= pushed[i] <= 1000`
- `pushed` 的所有元素 **互不相同**
- `popped.length == pushed.length`
- `popped` 是 `pushed` 的一个排列

## 解题思路 ：

**栈模拟法：** 用一个辅助栈来模拟整个 push/pop 过程。

核心思想很直接——按照 `pushed` 的顺序依次将元素压入栈中，每次压入后检查栈顶元素是否等于 `popped` 序列中当前待匹配的元素：如果相等就弹出栈顶，并将 `popped` 的指针后移，用 while 循环反复进行这个"检查-弹出"过程。最终如果栈为空，说明序列合法。

以示例 1 为例，`pushed=[1,2,3,4,5], popped=[4,5,3,2,1]`：

1. 依次 push 1, 2, 3, 4 → 栈 `[1,2,3,4]`，栈顶 4 == popped[0]，弹出 → `[1,2,3]`
2. push 5 → `[1,2,3,5]`，栈顶 5 == popped[1]，弹出 → `[1,2,3]`
3. 栈顶 3 == popped[2]，弹出 → `[1,2]`
4. 栈顶 2 == popped[3]，弹出 → `[1]`
5. 栈顶 1 == popped[4]，弹出 → `[]`
6. 栈为空，返回 true

以示例 2 为例，`pushed=[1,2,3,4,5], popped=[4,3,5,1,2]`：

1. 依次 push 1, 2, 3, 4 → 栈 `[1,2,3,4]`，弹出 4 → `[1,2,3]`
2. 栈顶 3 == popped[1]，弹出 → `[1,2]`
3. push 5 → `[1,2,5]`，弹出 5 → `[1,2]`
4. popped 下一个要 1，但栈顶是 2，不匹配且 pushed 已遍历完
5. 栈不为空，返回 false


## 代码实现

## PHP 解法

```php
class Solution {

    /**
     * @param Integer[] $pushed
     * @param Integer[] $popped
     * @return Boolean
     */
    function validateStackSequences($pushed, $popped) {
        $stack = [];
        $j = 0; // popped 数组的指针

        foreach ($pushed as $val) {
            // 将当前元素压入栈
            $stack[] = $val;

            // 当栈顶元素等于 popped 当前待匹配元素时，弹出栈顶
            while (!empty($stack) && end($stack) === $popped[$j]) {
                array_pop($stack);
                $j++;
            }
        }

        // 栈为空说明序列合法
        return empty($stack);
    }
}
```

**PHP 语法要点：**

- `$stack[] = $val` 是 PHP 中向数组末尾追加元素的简写方式，等价于 `array_push($stack, $val)`，但性能更好，因为避免了函数调用的开销。
- `end($stack)` 将数组内部指针移动到最后一个元素并返回其值，用来获取栈顶元素。
- `empty($stack)` 判断数组是否为空，等价于 `count($stack) === 0`。

## Go 解法

```go
func validateStackSequences(pushed []int, popped []int) bool {
    stack := []int{}
    j := 0 // popped 切片的指针

    for _, val := range pushed {
        // 将当前元素压入栈
        stack = append(stack, val)

        // 当栈顶元素等于 popped 当前待匹配元素时，弹出栈顶
        for len(stack) > 0 && stack[len(stack)-1] == popped[j] {
            stack = stack[:len(stack)-1] // 弹出栈顶
            j++
        }
    }

    // 栈为空说明序列合法
    return len(stack) == 0
}
```

**Go 语法要点（对比 PHP）：**

- Go 没有内置的 `push`/`pop` 函数，用 `append(slice, val)` 模拟入栈，用 `slice[:len(slice)-1]` 模拟出栈。这和 PHP 的 `array_push()` / `array_pop()` 不同，需要手动操作切片的长度。
- Go 中获取栈顶元素用 `stack[len(stack)-1]`，类似 PHP 的 `end($stack)`，但 Go 不需要移动内部指针。
- Go 的 `for _, val := range pushed` 相当于 PHP 的 `foreach ($pushed as $val)`，是 Go 中遍历切片的标准方式。
- Go 判断切片是否为空用 `len(stack) == 0`，对应 PHP 的 `empty($stack)`。

## 关键点

这道题的核心在于**贪心**的思想：由于元素入栈顺序是固定的（按 pushed 数组顺序），所以在任何时刻，如果栈顶元素恰好是 popped 序列中下一个要弹出的元素，那就**必须**立刻弹出它——因为如果不弹，后面压入的新元素会覆盖在它上面，就更不可能先弹出了。

这个"能弹就弹"的贪心策略保证了模拟过程的正确性。

另外注意 Go 和 PHP 在栈操作上的差异：PHP 数组底层是哈希表，提供了丰富的数组操作函数（`array_push`、`array_pop`、`end` 等）；Go 的切片是连续内存的动态数组，操作更底层但性能更好，需要手动通过切片表达式来完成 push/pop。

## 复杂度分析

- 时间复杂度 O(N) ：每个元素最多入栈一次、出栈一次，总共操作不超过 2N 次。
- 空间复杂度 O(N) ：最坏情况下所有元素都在栈中（例如 pushed 和 popped 顺序完全相同时，所有元素先全部入栈再依次弹出）。

#leetcode #栈 #模拟 #贪心
