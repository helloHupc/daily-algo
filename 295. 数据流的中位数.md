
**题目链接**：[https://leetcode.cn/problems/find-median-from-data-stream/description](https://leetcode.cn/problems/find-median-from-data-stream/description)

---

## 题目描述

**中位数**是有序整数列表中的中间值。如果列表的大小是偶数，则没有中间值，中位数是两个中间值的平均值。

- 例如 `arr = [2,3,4]` 的中位数是 `3` 。
- 例如 `arr = [2,3]` 的中位数是 `(2 + 3) / 2 = 2.5` 。

实现 MedianFinder 类:

- `MedianFinder()` 初始化 `MedianFinder` 对象。
    
- `void addNum(int num)` 将数据流中的整数 `num` 添加到数据结构中。
    
- `double findMedian()` 返回到目前为止所有元素的中位数。与实际答案相差 `10-5` 以内的答案将被接受。
    

**示例 1：**

**输入**
["MedianFinder", "addNum", "addNum", "findMedian", "addNum", "findMedian"]
[[], [1], [2], [], [3], []]
**输出**
[null, null, null, 1.5, null, 2.0]

**解释**
MedianFinder medianFinder = new MedianFinder();
medianFinder.addNum(1);    // arr = [1]
medianFinder.addNum(2);    // arr = [1, 2]
medianFinder.findMedian(); // 返回 1.5 ((1 + 2) / 2)
medianFinder.addNum(3);    // arr[1, 2, 3]
medianFinder.findMedian(); // return 2.0

**提示:**

- `-105 <= num <= 105`
- 在调用 `findMedian` 之前，数据结构中至少有一个元素
- 最多 `5 * 104` 次调用 `addNum` 和 `findMedian`

## 解题思路 ：

引入**堆**来实现。维护一个大顶堆，一个小顶堆，把数据隔成2部分，来实现获取到中位数。


## 代码实现

## PHP 解法

```php
class MedianFinder {
    /**
     */
    private SplMinHeap $right;  //小顶堆，保存较大的数据  右边
    private SplMaxHeap $left;  // 大顶堆，保存较小的数据  左边
    function __construct() {
        // 右边数据多一个
        $this->right = new SplMinHeap();
        $this->left = new SplMaxHeap();
    }
  
    /**
     * @param Integer $num
     * @return NULL
     */
    function addNum($num) {
        // 两个堆数据相等时，将数据放入小顶堆。也就是右边
        if ($this->right->count() == $this->left->count()){
            // 放入小顶堆之前，先将数据放入大顶堆中，然后把大顶堆的最大值放入小顶堆中。
            $this->left->insert($num);
            $max_of_left = $this->left->extract();
            $this->right->insert($max_of_left);
        }else{
            // 两边数量不一致时，肯定是左边少一个，需要把数据放入左边。
            // 先将数据放入右边，然后把右边最小的数据写入左边
            $this->right->insert($num);
            $min_of_right = $this->right->extract();
            $this->left->insert($min_of_right);
        }
    }
  
    /**
     * @return Float
     */
    function findMedian() {
        // 如果两个堆数据量一致，则取左边最大值，右边最小值，取平均数
        // 如果两个堆数量不一致，因为是右边数据多一个，所以直接取右边的最小值即可
        if ($this->right->count() == $this->left->count()){
            $leftMax = $this->left->top();
            $rightMin = $this->right->top();
            return ($leftMax+$rightMin)/2;
        }else{
            return $this->right->top();
        }
    }
}

/**
 * Your MedianFinder object will be instantiated and called as such:
 * $obj = MedianFinder();
 * $obj->addNum($num);
 * $ret_2 = $obj->findMedian();
 */
```
## 关键点

小顶堆，保存较大的数据  右边
大顶堆，保存较小的数据  左边

## 复杂度分析

- 时间复杂度 O(logN) ：
	查找中位数 O(1) ： 获取堆顶元素使用 O(1) 时间。
	添加数字 O(logN) ： 堆的插入和弹出操作使用 O(logN) 时间。
	
- 空间复杂度 O(N) ： 其中 N 为数据流中的元素数量，小顶堆 A 和大顶堆 B 最多同时保存 N 个元素。

#leetcode #堆 #设计 #二分查找
