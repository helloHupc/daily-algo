
**题目链接**：[https://leetcode.cn/problems/merge-two-sorted-lists/description](https://leetcode.cn/problems/merge-two-sorted-lists/description)

---

## 题目描述

将两个升序链表合并为一个新的 **升序** 链表并返回。新链表是通过拼接给定的两个链表的所有节点组成的。 

**示例 1：**

![](https://assets.leetcode.com/uploads/2020/10/03/merge_ex1.jpg)

> **输入：** l1 = [1,2,4], l2 = [1,3,4]  
> **输出：** [1,1,2,3,4,4]

**示例 2：**

> **输入：** l1 = [], l2 = []  
> **输出：** []

**示例 3：**

> **输入：** l1 = [], l2 = [0]  
> **输出：** [0]

**提示：**

- 两个链表的节点数目范围是 [0, 50]
- -100 <= Node.val <= 100
- l1 和 l2 均按 **非递减顺序** 排列

## 解题思路 ：双指针 + 哨兵节点

本题的核心在于如何优雅地处理头节点和边界情况。最推荐的方法是使用 **哨兵节点（Dummy Node）** 配合 **双指针**。
#### 核心步骤：

1. **创建哨兵节点 (dummy)**：创建一个虚拟头节点，它的值不重要。目的是为了统一代码逻辑，避免处理“新链表头节点究竟是 l1 还是 l2”这种繁琐的边界判断。
2. **双指针遍历**：使用一个游标指针 cur 指向 dummy，同时遍历 l1 和 l2。
3. **择优录取**：比较 l1 和 l2 当前节点的值，将较小值的节点接在 cur 后面，并将对应的指针后移一位。
4. **处理剩余**：当循环结束时，必定有一个链表未遍历完（或刚好都完）。直接将 cur 的 next 指向剩余的非空链表即可（因为链表本身有序）。
5. **返回结果**：返回 dummy.next 即为合并后的真正头节点。

## 代码实现

## PHP 解法

```php
/**
 * Definition for a singly-linked list.
 * class ListNode {
 *     public $val = 0;
 *     public $next = null;
 *     function __construct($val = 0, $next = null) {
 *         $this->val = $val;
 *         $this->next = $next;
 *     }
 * }
 */
class Solution {

    /**
     * @param ListNode $list1
     * @param ListNode $list2
     * @return ListNode
     */
    function mergeTwoLists($list1, $list2) {
        $newList = new ListNode();
        $cur = $newList;

        while($list1 != null && $list2 != null){
            if($list1->val < $list2->val){
                $cur->next = $list1;
                $list1 = $list1->next;
            }else{
                $cur->next = $list2;
                $list2 = $list2->next;
            }
            $cur = $cur->next;
        }
        $cur->next = $list1 != null ? $list1 : $list2;
        
        return $newList->next;
        
    }
}
```

## Go 解法

```go
/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */
func mergeTwoLists(list1 *ListNode, list2 *ListNode) *ListNode {
    dum := &ListNode{Val:0}
    cur := dum

    for list1 != nil && list2 != nil {
        if list1.Val < list2.Val {
            cur.Next = list1
            list1 = list1.Next
        }else{
            cur.Next = list2
            list2 = list2.Next
        }
        cur = cur.Next
    }

    if list1 != nil {
        cur.Next = list1
    }else{
        cur.Next = list2
    }

    return dum.Next
}
```
## 关键点

#### 为什么需要 cur := dum？

核心点在于，dum是指针变量`dum := &ListNode{Val:0}`。所以`cur := dum`并不是复制了一个链表，他们指向的是同一个内存地址。

#### 形象比喻
`cur` 是一个**游标**，它在一条**不断增长的链表上移动**。这条链表的头是 `dum` 所指向的哨兵节点。

- **dum 是“指挥部”**：原地不动，守住入口。
- **cur 是“施工队”**：从指挥部出发，一边铺路（连接节点）一边向前走。
- 最后，指挥部（dum）指的路（next），就是铺好的整条路。

## 语言特性笔记：PHP 对象引用

在 PHP 代码中：
```php
$newList = new ListNode();
$cur = $newList;
```

**核心知识点：**  
PHP 5+ 中，对象变量存储的不是整个对象的值，而是指向该对象的**标识符（Object Identifier）**。  
当执行 $cur = $newList 时，系统复制了一份“标识符”，这使得 $cur 和 $newList 实际上指向了**内存中的同一个对象实例**。

## 复杂度分析

- 时间复杂度 O(M+N) ： M,N 分别是两个链表的长度。我们需要遍历两个链表的每一个节点一次。
- 空间复杂度 O(1) ： 节点引用 dum , cur 使用常数大小的额外空间。

#leetcode #链表 #递归
