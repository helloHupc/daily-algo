
**题目链接**：[https://leetcode.cn/problems/find-minimum-in-rotated-sorted-array-ii/description](https://leetcode.cn/problems/find-minimum-in-rotated-sorted-array-ii/description)

---
## 题目描述

已知一个长度为 `n` 的数组，预先按照升序排列，经由 `1` 到 `n` 次**旋转**后，得到输入数组。

例如，原数组 `nums = [0,1,4,4,5,6,7]` 在变化后可能得到：

- 若旋转 `4` 次，则可以得到 `[4,5,6,7,0,1,4]`
- 若旋转 `7` 次，则可以得到 `[0,1,4,4,5,6,7]`

注意，数组 `[a[0], a[1], a[2], ..., a[n-1]]` **旋转一次**的结果为数组 `[a[n-1], a[0], a[1], a[2], ..., a[n-2]]` 。

给你一个可能存在**重复**元素值的数组 `nums`，它原来是一个升序排列的数组，并按上述情形进行了多次旋转。请你找出并返回数组中的**最小元素**。

你必须尽可能减少整个过程的操作步骤。

**示例 1：**

```
输入：nums = [1,3,5]
输出：1
```

**示例 2：**

```
输入：nums = [2,2,2,0,1]
输出：0
```

**提示：**

- `n == nums.length`
- `1 <= n <= 5000`
- `-5000 <= nums[i] <= 5000`
- `nums` 原来是一个升序排序的数组，并进行了 `1` 至 `n` 次旋转

**进阶：** 这道题与[[153. 寻找旋转排序数组中的最小值]]类似，但 `nums` 可能包含重复元素。允许重复会影响算法的时间复杂度吗？会如何影响，为什么？

## 解题思路

### 核心思想：二分查找 + 重复元素处理

这道题是 [[153. 寻找旋转排序数组中的最小值]] 的进阶版，区别在于数组中**可能包含重复元素**。

#### 基础版回顾（无重复元素）

对于无重复的旋转排序数组，二分查找的核心是比较 `nums[mid]` 和 `nums[right]`：

```
旋转数组特点：[4,5,6,7,0,1,2]
                     ↑mid    ↑right
```

- 如果 `nums[mid] < nums[right]`：说明 `mid` 到 `right` 是递增的，最小值在左半部分（包含 `mid`），`right = mid`
- 如果 `nums[mid] > nums[right]`：说明最小值在右半部分（不包含 `mid`），`left = mid + 1`

#### 有重复元素时的困境

当存在重复元素时，会出现 `nums[mid] == nums[right]` 的情况：

```
例子：[1, 0, 1, 1, 1]
      left  mid    right
```

此时 `nums[mid] == nums[right] == 1`，我们无法判断最小值在左边还是右边。

```
例子：[1, 1, 1, 0, 1]
      left  mid    right
```

也是 `nums[mid] == nums[right]`，但最小值在 `mid` 和 `right` 之间。

#### 解决办法

当 `nums[mid] == nums[right]` 时，我们无法做出确定性判断，但可以**缩小范围**——将 `right` 左移一位（`right--`），因为：

- 即使 `nums[right]` 就是最小值，它的副本 `nums[mid]` 还在搜索区间内
- 这个操作不会丢失最小值

```
步骤示例：nums = [1, 0, 1, 1, 1]

第1轮：left=0, right=4, mid=2
       nums[2]=1, nums[4]=1 → nums[mid] == nums[right] → right--（right=3）

第2轮：left=0, right=3, mid=1
       nums[1]=0, nums[3]=1 → nums[mid] < nums[right] → right = mid（right=1）

第3轮：left=0, right=1, mid=0
       nums[0]=1, nums[1]=0 → nums[mid] > nums[right] → left = mid + 1（left=1）

第4轮：left=1, right=1 → 返回 nums[1]=0 ✓
```

#### 算法流程

1. 初始化 `left = 0`，`right = n - 1`
2. 循环直到 `left < right`：
   - 计算 `mid = left + (right - left) / 2`
   - 如果 `nums[mid] < nums[right]`：最小值在左半（含 mid），`right = mid`
   - 如果 `nums[mid] > nums[right]`：最小值在右半（不含 mid），`left = mid + 1`
   - 如果 `nums[mid] == nums[right]`：无法判断，安全收缩，`right--`
3. 返回 `nums[left]`

### 复杂度分析

- **时间复杂度**：
  - 平均情况：O(log n) — 大部分情况下二分查找正常工作
  - 最坏情况：O(n) — 当大量重复元素时（如 `[1,1,1,1,1]`），会退化为线性查找
- **空间复杂度**：O(1) — 只用了几个变量

### 为什么重复元素会影响时间复杂度？

因为当 `nums[mid] == nums[right]` 时，我们只能通过 `right--` 来逐个排除元素，无法像无重复时那样直接砍掉一半搜索范围。

最坏情况是数组所有元素都相等，如 `[1,1,1,1,1]`：

```
left=0, right=4, mid=2 → nums[2]==nums[4] → right=3
left=0, right=3, mid=1 → nums[1]==nums[3] → right=2
left=0, right=2, mid=1 → nums[1]==nums[2] → right=1
left=0, right=1, mid=0 → nums[0]==nums[1] → right=0
left=0, right=0 → 返回 nums[0]=1
```

这种情况下，每次都只缩小一位，总操作次数为 O(n)。

## PHP 解法

```php
class Solution {

    /**
     * @param Integer[] $nums
     * @return Integer
     */
    function findMin($nums) {
        $left = 0;
        $right = count($nums) - 1;

        while ($left < $right) {
            $mid = $left + intval(($right - $left) / 2);

            if ($nums[$mid] < $nums[$right]) {
                // mid 到 right 递增，最小值在左半（含 mid）
                $right = $mid;
            } elseif ($nums[$mid] > $nums[$right]) {
                // 最小值在右半（不含 mid）
                $left = $mid + 1;
            } else {
                // nums[mid] == nums[right]，无法判断，缩小右边界
                $right--;
            }
        }

        return $nums[$left];
    }
}
```

**说明：**
- 循环条件是 `$left < $right`（不是 `<=`），因为当 `left == right` 时就找到了最小值
- `$mid` 计算用 `intval()` 取整，也可以用 `>> 1` 位运算：`$mid = $left + (($right - $left) >> 1)`
- 三种分支对应三种比较结果，逻辑清晰

## Go 解法

```go
func findMin(nums []int) int {
    left, right := 0, len(nums)-1

    for left < right {
        mid := left + (right-left)/2

        if nums[mid] < nums[right] {
            // mid 到 right 递增，最小值在左半（含 mid）
            right = mid
        } else if nums[mid] > nums[right] {
            // 最小值在右半（不含 mid）
            left = mid + 1
        } else {
            // nums[mid] == nums[right]，无法判断，缩小右边界
            right--
        }
    }

    return nums[left]
}
```

**说明：**
- `left + (right-left)/2` 是 Go 的整数除法，自动截断，无需取整
- 循环条件 `left < right`，结束时 `left == right` 即为最小值位置
- `right--` 操作安全，因为即使 `nums[right]` 是最小值，其副本 `nums[mid]` 还在区间内

## Go 与 PHP 差异总结

| 对比项 | PHP | Go |
|--------|-----|-----|
| **类型系统** | 弱类型，数组元素自动推断 | 强类型，`[]int` 明确元素类型 |
| **取整除法** | `/` 返回 float，需 `intval()` 转换 | 整数除法直接截断，无需额外处理 |
| **数组长度** | `count($nums)` — 函数调用 | `len(nums)` — 内置函数 |
| **变量声明** | `$left = 0` — 直接赋值 | `left := 0` — 短变量声明 (`:=`) |
| **else if** | `elseif` 或 `else if` 均可 | 必须写成 `else if`（中间有空格） |
| **数组越界** | `$nums[$right]` 访问不存在下标时返回 `null` + 警告 | `nums[right]` 编译期无法检查，运行期直接 panic |

### 经典踩坑点

1. **循环条件 `left < right` vs `left <= right`：**
   - 本题用 `left < right`，因为当左右指针相遇时，`nums[left]` 就是最小值
   - 如果用 `<=`，最后会死循环（因为 `right = mid` 而不是 `mid - 1`）
   - 判断标准：看区间收缩方式——如果用 `right = mid`（保留 mid），就用 `left < right`；如果用 `right = mid - 1`（排除 mid），就用 `left <= right`

2. **`nums[mid] < nums[right]` 时为什么是 `right = mid` 而不是 `right = mid - 1`？**
   - 因为 `mid` 自身可能就是最小值（比如旋转到了升序段开头），不能排除
   - 例子：`[4,5,6,7,0,1,2]`，`mid=3`（值为 7），`nums[3] > nums[6]` → `left = mid + 1 = 4`（指向 0）
   - 但如果 `mid` 已经是升序段中，`mid` 可能就是最小值

3. **对比不重复版本的差异：**
   - 无重复：`nums[mid] == nums[right]` 不会出现，无需处理
   - 有重复：多了一个 `else` 分支来处理相等的情况，可能退化为 O(n)

## 扩展知识：与「无重复元素」版本的对比

| 对比维度 | 153. 无重复元素 | 154. 有重复元素 |
|----------|----------------|----------------|
| 比较逻辑 | `if/else` 两种分支 | `if/else if/else` 三种分支 |
| 相等处理 | 不可能出现 | `right--` 安全收缩 |
| 最坏时间复杂度 | O(log n) | O(n) |
| 平均时间复杂度 | O(log n) | O(log n) |
| 代码差异 | 少一个 `else` 分支 | 多了 `else { right-- }` |

**核心区别一句话：** 有重复元素时，`nums[mid] == nums[right]` 的情况让我们无法二分，只能线性收缩，导致最坏情况退化为 O(n)。

#leetcode #数组 #二分查找 #binary-search #旋转数组
