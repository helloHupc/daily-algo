
**题目链接**：[https://leetcode.cn/problems/longest-substring-without-repeating-characters/description](https://leetcode.cn/problems/longest-substring-without-repeating-characters/description)

---

## 题目描述

给定一个字符串 `s`，请你找出其中不含有重复字符的**最长子串**的长度。

**示例 1：**

```
输入: s = "abcabcbb"
输出: 3
解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。
```

**示例 2：**

```
输入: s = "bbbbb"
输出: 1
解释: 因为无重复字符的最长子串是 "b"，所以其长度为 1。
```

**示例 3：**

```
输入: s = "pwwkew"
输出: 3
解释: 因为无重复字符的最长子串是 "wke"，所以其长度为 3。
      请注意，你的答案必须是子串的长度，"pwke" 是一个子序列，不是子串。
```

**提示：**

- `0 <= s.length <= 5 * 10^4`
- `s` 由英文字母、数字、符号和空格组成

## 解题思路

### 滑动窗口

用一个"窗口"（左右指针）框住一段子串，保证窗口内没有重复字符：

- 右指针不断右移，扩大窗口
- 如果遇到重复字符，左指针右移，缩小窗口，直到重复字符被移出
- 每次移动后更新最大长度

用 map 记录每个字符最近出现的位置，左指针可以直接跳到重复字符的下一个位置。

```
s = "abcabcbb"

right=0, ch='a': 窗口[a]       长度1
right=1, ch='b': 窗口[a,b]     长度2
right=2, ch='c': 窗口[a,b,c]   长度3 ← 最大
right=3, ch='a': a重复，左指针跳到1，窗口[b,c,a] 长度3
right=4, ch='b': b重复，左指针跳到2，窗口[c,a,b] 长度3
right=5, ch='c': c重复，左指针跳到3，窗口[a,b,c] 长度3
right=6, ch='b': b重复，左指针跳到5，窗口[c,b]   长度2
right=7, ch='b': b重复，左指针跳到8，窗口[b]     长度1

结果：3
```

**时间复杂度：** O(n)，每个字符最多被访问两次

**空间复杂度：** O(min(n, m))，m 是字符集大小

## Go 解法

```go
func lengthOfLongestSubstring(s string) int {
    charIndex := make(map[rune]int)
    maxLength := 0
    left := 0

    for right, ch := range s {
        // 如果字符在窗口内出现过，左指针跳到重复字符的下一个位置
        if lastIndex, ok := charIndex[ch]; ok && lastIndex >= left {
            left = lastIndex + 1
        }
        // 更新字符的最新位置
        charIndex[ch] = right
        // 计算当前窗口长度
        length := right - left + 1
        if length > maxLength {
            maxLength = length
        }
    }
    return maxLength
}
```

**说明：** `charIndex` 记录每个字符最近出现的索引。`lastIndex >= left` 确保重复字符在当前窗口内（而不是窗口左边已经被移出的位置）。窗口长度 = `right - left + 1`。

## PHP 解法

```php
class Solution {

    /**
     * @param String $s
     * @return Integer
     */
    function lengthOfLongestSubstring($s) {
        $charIndex = [];
        $maxLength = 0;
        $left = 0;
        $len = strlen($s);

        for ($right = 0; $right < $len; $right++) {
            $ch = $s[$right];
            // 如果字符在窗口内出现过，左指针跳到重复字符的下一个位置
            if (isset($charIndex[$ch]) && $charIndex[$ch] >= $left) {
                $left = $charIndex[$ch] + 1;
            }
            // 更新字符的最新位置
            $charIndex[$ch] = $right;
            // 计算当前窗口长度
            $length = $right - $left + 1;
            if ($length > $maxLength) {
                $maxLength = $length;
            }
        }
        return $maxLength;
    }
}
```

**说明：** `$charIndex` 记录每个字符最近出现的索引。`$charIndex[$ch] >= $left` 确保重复字符在当前窗口内。PHP 用 `isset()` 检查键是否存在。

#leetcode #哈希表 #滑动窗口
