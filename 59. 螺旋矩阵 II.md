
**题目链接**：[https://leetcode.cn/problems/spiral-matrix-ii/description](https://leetcode.cn/problems/spiral-matrix-ii/description)

---

## 题目描述

给你一个正整数 `n` ，生成一个包含 `1` 到 `n²` 所有元素，且元素按顺时针顺序螺旋排列的 `n x n` 正方形矩阵 `matrix`。

**示例 1：**

**输入：**
`n = 3`
**输出：**
`[[1,2,3],[8,9,4],[7,6,5]]`

**示例 2：**

**输入：**
`n = 1`
**输出：**
`[[1]]`

**提示：**

- `1 <= n <= 20`

## 解题思路 ：

**边界收缩法：** 和 [[54. 螺旋矩阵]] 思路完全一样，只是从"读取"变成"填写"。

维护四个边界 `top`、`bottom`、`left`、`right`，用一个计数器从 1 到 n²，按顺时针方向依次填入矩阵：

1. **从左到右**：沿 `top` 行填入数字，走完后 `top++`
2. **从上到下**：沿 `right` 列填入数字，走完后 `right--`
3. **从右到左**：沿 `bottom` 行填入数字，走完后 `bottom--`
4. **从下到上**：沿 `left` 列填入数字，走完后 `left++`

循环直到所有 n² 个数字填完。

以 `n = 3` 为例：

```
初始：top=0, bottom=2, left=0, right=2, num=1

第一圈：
→ 沿 top 行(0): matrix[0][0]=1, matrix[0][1]=2, matrix[0][2]=3   top=1, num=4
↓ 沿 right 列(2): matrix[1][2]=4, matrix[2][2]=5                  right=1, num=6
← 沿 bottom 行(2): matrix[2][1]=6, matrix[2][0]=7                 bottom=1, num=8
↑ 沿 left 列(0): matrix[1][0]=8                                   left=1, num=9

第二圈：
→ 沿 top 行(1): matrix[1][1]=9                                    top=2, num=10
   此时 top > bottom，结束

结果：[[1,2,3],[8,9,4],[7,6,5]]
```

**注意：** 由于本题是 n×n 方阵，不存在 54 题中单行/单列的重复遍历问题，所以不需要额外的边界检查。但加上也不影响正确性。

## 代码实现

## PHP 解法

```php
class Solution {

    /**
     * @param Integer $n
     * @return Integer[][]
     */
    function generateMatrix($n) {
        // 初始化 n×n 矩阵，全部填 0
        $matrix = array_fill(0, $n, array_fill(0, $n, 0));

        $top = 0;
        $bottom = $n - 1;
        $left = 0;
        $right = $n - 1;
        $num = 1; // 从 1 开始填

        while ($top <= $bottom && $left <= $right) {
            // 从左到右，沿 top 行
            for ($i = $left; $i <= $right; $i++) {
                $matrix[$top][$i] = $num++;
            }
            $top++;

            // 从上到下，沿 right 列
            for ($i = $top; $i <= $bottom; $i++) {
                $matrix[$i][$right] = $num++;
            }
            $right--;

            // 从右到左，沿 bottom 行
            for ($i = $right; $i >= $left; $i--) {
                $matrix[$bottom][$i] = $num++;
            }
            $bottom--;

            // 从下到上，沿 left 列
            for ($i = $bottom; $i >= $top; $i--) {
                $matrix[$i][$left] = $num++;
            }
            $left++;
        }

        return $matrix;
    }
}
```

**PHP 语法要点：**

- `array_fill(0, $n, array_fill(0, $n, 0))` 创建 n×n 的二维数组，内层 `array_fill` 生成一行 n 个 0，外层 `array_fill` 复制 n 行。
- `$num++` 是后缀自增，先把当前值赋给矩阵，再自增。等价于 `$matrix[$top][$i] = $num; $num = $num + 1;`，但更简洁。

## Go 解法

```go
func generateMatrix(n int) [][]int {
    // 初始化 n×n 矩阵
    matrix := make([][]int, n)
    for i := range matrix {
        matrix[i] = make([]int, n)
    }

    top := 0
    bottom := n - 1
    left := 0
    right := n - 1
    num := 1

    for top <= bottom && left <= right {
        // 从左到右，沿 top 行
        for i := left; i <= right; i++ {
            matrix[top][i] = num
            num++
        }
        top++

        // 从上到下，沿 right 列
        for i := top; i <= bottom; i++ {
            matrix[i][right] = num
            num++
        }
        right--

        // 从右到左，沿 bottom 行
        for i := right; i >= left; i-- {
            matrix[bottom][i] = num
            num++
        }
        bottom--

        // 从下到上，沿 left 列
        for i := bottom; i >= top; i-- {
            matrix[i][left] = num
            num++
        }
        left++
    }

    return matrix
}
```

**Go 语法要点（对比 PHP）：**

- Go 没有 `array_fill` 这样的函数，创建二维切片需要两步：先用 `make([][]int, n)` 创建外层切片，再循环为每一行 `make([]int, n)`。PHP 一行 `array_fill` 就搞定了，这是 Go 更底层的地方。
- Go 没有 `$num++` 作为表达式的用法。PHP 中 `$matrix[$top][$i] = $num++` 一行搞定，Go 必须拆成两行：先赋值 `matrix[top][i] = num`，再自增 `num++`。这是因为 Go 的 `++` 是语句（statement），不是表达式（expression），不能出现在赋值右侧。
- `for i := range matrix` 是 Go 遍历切片的标准方式，这里用来初始化每一行。

## 关键点

这道题和第 54 题是一对"镜像题"：54 题是按螺旋顺序**读取**矩阵元素，本题是按螺旋顺序**写入**数字。核心算法完全一样——四个边界顺时针收缩。

区别在于：本题是 n×n 方阵，四条边长度对称，不会出现 54 题中 m≠n 时单行/单列重复遍历的问题。当 n 为奇数时，最内圈只剩一个中心元素（如 n=3 时的 `9`），第一步填入后 top > bottom，自然退出。

## 复杂度分析

- 时间复杂度 O(N²) ：需要填入 N² 个数字，每个数字填一次。
- 空间复杂度 O(1) ：除了返回的矩阵外，只用了几个边界变量和计数器（结果矩阵不算额外空间）。

#leetcode #数组 #模拟 #矩阵
