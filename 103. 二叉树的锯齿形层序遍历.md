
**题目链接**：[https://leetcode.cn/problems/binary-tree-zigzag-level-order-traversal/description](https://leetcode.cn/problems/binary-tree-zigzag-level-order-traversal/description)

---
## 题目描述

给你二叉树的根节点 `root`，返回其节点值的**锯齿形层序遍历**。（即先从左往右，再从右往左进行下一层遍历，以此类推，层与层之间交替进行）。

**示例 1：**

```
输入：root = [3,9,20,null,null,15,7]
输出：[[3],[20,9],[15,7]]
```

**示例 2：**

```
输入：root = [1]
输出：[[1]]
```

**示例 3：**

```
输入：root = []
输出：[]
```

**提示：**

- 树中节点数目在范围 `[0, 2000]` 内
- `-100 <= Node.val <= 100`

## 解题思路

### 核心思想：BFS + 方向标记

锯齿形层序遍历是标准层序遍历（[[LeetCode/102. 二叉树的层序遍历]]）的变体。唯一的区别在于：**每层的遍历方向交替变化**。

- 第 0 层（根节点）：从左到右
- 第 1 层：从右到左
- 第 2 层：从左到右
- …

关键是理解：**节点的入队顺序不变**（永远是先左后右），**变的只是当前层的节点值收集顺序**。

### 两种实现思路

#### 思路一：收集后反转（PHP 适用）

标准 BFS 收集每一层，如果当前层是反向，直接 `array_reverse()` 反转整个层数组。思路最直观。

#### 思路二：按位置填入（Go 适用）

收集层时，根据方向决定填入位置：
- 从左到右：`level[$i]` 正常填充
- 从右到左：`level[size - 1 - $i]` 从尾部向前填充

这种做法**避免了反转的额外开销**，更高效。

---

### 算法流程

1. 初始化结果数组 `result = []`，队列 `queue = [root]`，方向标记 `leftToRight = true`
2. 如果 `root == null`，直接返回 `[]`
3. 循环直到队列为空：
   - 记录当前队列大小 `size = count(queue)`
   - 创建当前层的数组 `level`
   - 循环 `size` 次：
     - 出队一个节点
     - **根据方向**将节点值填入 `level` 的对应位置
     - 左子节点、右子节点入队（**顺序始终是先左后右**）
   - 将 `level` 加入 `result`
   - **翻转方向** `leftToRight = !leftToRight`
4. 返回 `result`

### 复杂度分析

- **时间复杂度**：O(n) — 每个节点恰好入队出队一次
- **空间复杂度**：O(n) — 队列中最多存储一层的节点，最坏情况（完全二叉树最底层）有约 n/2 个节点

## PHP 解法

```php
class Solution {

    /**
     * @param TreeNode $root
     * @return Integer[][]
     */
    function zigzagLevelOrder($root) {
        if ($root === null) {
            return [];
        }

        $result = [];
        $queue = [$root];
        $leftToRight = true;

        while (!empty($queue)) {
            $size = count($queue);
            $level = [];

            for ($i = 0; $i < $size; $i++) {
                $node = array_shift($queue);
                $level[] = $node->val;

                if ($node->left !== null) {
                    $queue[] = $node->left;
                }
                if ($node->right !== null) {
                    $queue[] = $node->right;
                }
            }

            // 如果是反向层，直接反转数组
            if (!$leftToRight) {
                $level = array_reverse($level);
            }

            $result[] = $level;
            $leftToRight = !$leftToRight; // 翻转方向
        }

        return $result;
    }
}
```

**说明：**
- 相比标准层序遍历（102），只多了两行：方向标记 `$leftToRight` 和条件反转 `array_reverse()`
- PHP 的 `array_reverse()` 返回新数组，时间复杂度 O(k)（k 为当前层节点数），总时间复杂度仍为 O(n)
- 可以选择用 `SplQueue` 替代数组 + `array_shift()` 获得更好的出队性能

## Go 解法

```go
/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val int
 *     Left *TreeNode
 *     Right *TreeNode
 * }
 */

func zigzagLevelOrder(root *TreeNode) [][]int {
    if root == nil {
        return [][]int{}
    }

    result := make([][]int, 0)
    queue := []*TreeNode{root}
    leftToRight := true

    for len(queue) > 0 {
        size := len(queue)
        // 预分配长度为 size，后面直接用索引赋值
        level := make([]int, size)

        for i := 0; i < size; i++ {
            node := queue[0]
            queue = queue[1:]

            // 根据方向决定填入位置
            if leftToRight {
                level[i] = node.Val       // 从左到右：正常顺序
            } else {
                level[size-1-i] = node.Val // 从右到左：反向填充
            }

            if node.Left != nil {
                queue = append(queue, node.Left)
            }
            if node.Right != nil {
                queue = append(queue, node.Right)
            }
        }

        result = append(result, level)
        leftToRight = !leftToRight // 翻转方向
    }

    return result
}
```

**说明：**
- Go 这里用了"按位置填入"的思路，而不是收集完再反转——这比 PHP 的 `array_reverse` 更高效（O(size) → O(1) 的单元素插入）
- `make([]int, size)` 创建了指定长度的切片，可以直接用索引赋值（而不是 `append`）
- 反向填充时 `level[size-1-i]`：i=0 填最右边，i=1 填倒数第二个，以此类推

## Go 与 PHP 差异总结

| 对比项 | PHP | Go |
|--------|-----|-----|
| **反向层实现** | 正常收集后用 `array_reverse()` 反转 | 按索引位置直接填入，`level[size-1-i] = val` |
| **预分配数组** | PHP 数组动态扩容，无需预分配 | `make([]int, size)` 预分配长度，直接索引赋值 |
| **数组填充** | `$level[] = $val` 尾部追加 | `level[i] = val` 或 `level[size-1-i] = val` 索引赋值 |
| **循环条件** | `while (!empty($queue))` | `for len(queue) > 0` |
| **nil/null** | `$root === null` | `root == nil` |
| **布尔取反** | `$leftToRight = !$leftToRight` | `leftToRight = !leftToRight`（语法几乎相同） |
| **队列出队** | `array_shift($queue)` 出队（O(n)） | `queue[1:]` 切片操作出队（O(1)） |

### 经典踩坑点

1. **Go 中 `make([]int, size)` 是创建长度为 size 的切片，不是容量！** 如果用 `make([]int, 0, size)` 再 `append`，反向填充的逻辑就无法直接通过索引实现了。区别巨大：
   - `make([]int, size)` → `level[0]` 到 `level[size-1]` 都可以直接赋值（索引赋值）
   - `make([]int, 0, size)` → 只能用 `append`，不能直接用索引（长度为 0）

2. **入队顺序千万不要变！** 锯齿形遍历改变的是**读取顺序**，不是入队顺序。所有节点始终按先左后右入队，否则整个遍历会乱。

3. **PHP 中 `array_reverse` 的陷阱：** `array_reverse()` 返回的是**新数组**，不会修改原数组。`$level = array_reverse($level)` 这样赋值才能生效。如果你写 `array_reverse($level)` 而不赋值，原数组不变。

4. **`size` 必须在循环开始时固定：** 很多新手会直接写 `for ($i = 0; $i < count($queue); $i++)`，但在循环内部队列长度会变化（入队新节点），这会导致遍历不完整。标准做法是先 `$size = count($queue)` 固定下来。

## 扩展知识

### DFS 递归解法（了解即可）

除了 BFS，也可以用 DFS（前序遍历）+ 层级标记来解决：

```go
func zigzagLevelOrder(root *TreeNode) [][]int {
    result := make([][]int, 0)
    dfs(root, 0, &result)
    return result
}

func dfs(node *TreeNode, depth int, result *[][]int) {
    if node == nil {
        return
    }
    // 首次到达该深度，创建新层
    if depth == len(*result) {
        *result = append(*result, []int{})
    }
    // 根据深度奇偶决定插入位置
    if depth%2 == 0 {
        (*result)[depth] = append((*result)[depth], node.Val)       // 左→右：尾部追加
    } else {
        (*result)[depth] = append([]int{node.Val}, (*result)[depth]...) // 右→左：头部插入
    }
    dfs(node.Left, depth+1, result)
    dfs(node.Right, depth+1, result)
}
```

DFS 方法不需要显式队列，空间复杂度 O(h)（h 为树高），但在反向层时头部插入 `append([]int{val}, level...)` 会创建新数组，实际开销比 BFS 的索引赋值大。作为了解即可，**面试推荐 BFS 解法**，因为面试官期待你理解层序遍历的核心是队列。

### 兄弟题目关联

- [[LeetCode/102. 二叉树的层序遍历]] — 本题的基础版本，建议先掌握
- **107. 二叉树的层序遍历 II** — 从底层到顶层的层序遍历（相当于 102 的结果反转）

#leetcode #二叉树 #BFS #层序遍历 #队列 #锯齿遍历
