
**题目链接**：[https://leetcode.cn/problems/two-sum-ii-input-array-is-sorted/description](https://leetcode.cn/problems/two-sum-ii-input-array-is-sorted/description)

---

## 题目描述

给你一个下标从 1 开始的整数数组 `numbers`，该数组已按非递减顺序排列，请你从数组中找出满足相加之和等于目标数 `target` 的两个数。

返回形式：以长度为 2 的整数数组 `[index1, index2]` 的形式返回这两个整数的下标。

**注意：**
- 每个输入只对应唯一的答案
- 不可以重复使用相同的元素
- 必须只使用常量级的额外空间

**示例 1：**

```
输入：numbers = [2,7,11,15], target = 9
输出：[1,2]
解释：2 与 7 之和等于目标数 9。
```

**示例 2：**

```
输入：numbers = [2,3,4], target = 6
输出：[1,3]
解释：2 与 4 之和等于目标数 6。
```

**示例 3：**

```
输入：numbers = [-1,0], target = -1
输出：[1,2]
解释：-1 与 0 之和等于目标数 -1。
```

**提示：**

- `2 <= numbers.length <= 3 * 10^4`
- `-1000 <= numbers[i] <= 1000`
- `numbers` 按非递减顺序排列
- `-1000 <= target <= 1000`
- 仅存在一个有效答案

## 解题思路

### 双指针

因为数组**已排序**，可以用左右指针：

- 左指针指向最小的数，右指针指向最大的数
- 如果和 **大于** target → 右指针左移（减小）
- 如果和 **小于** target → 左指针右移（增大）
- 如果和 **等于** target → 返回结果

```
numbers = [2,7,11,15], target = 9

初始：左=2(小), 右=15(大), 和=17 > 9 → 右左移
左=2, 右=11, 和=13 > 9 → 右左移
左=2, 右=7, 和=9 = 9 → 返回 [1,2] ✓
```

**注意：** 题目说"下标从 1 开始"是为了方便人类理解，代码中数组访问仍需从 0 开始，返回时 +1 即可。

**时间复杂度：** O(n)

**空间复杂度：** O(1)

## Go 解法

```go
func twoSum(numbers []int, target int) []int {
    left, right := 0, len(numbers)-1
    for left < right {
        sum := numbers[left] + numbers[right]
        if sum == target {
            return []int{left + 1, right + 1}  // 题目要求下标从1开始
        } else if sum < target {
            left++
        } else {
            right--
        }
    }
    return nil
}
```

**说明：** 数组访问从 0 开始（Go 语言规定），返回时 +1 满足题目"下标从1开始"的要求。

## PHP 解法

```php
class Solution {

    /**
     * @param Integer[] $numbers
     * @param Integer $target
     * @return Integer[]
     */
    function twoSum($numbers, $target) {
        $left = 0;
        $right = count($numbers) - 1;
        while ($left < $right) {
            $sum = $numbers[$left] + $numbers[$right];
            if ($sum == $target) {
                return [$left + 1, $right + 1];  // 题目要求下标从1开始
            } else if ($sum < $target) {
                $left++;
            } else {
                $right--;
            }
        }
        return null;
    }
}
```

**说明：** PHP 数组访问从 0 开始，返回时 +1 满足题目要求。

#leetcode #数组 #双指针 #二分查找
