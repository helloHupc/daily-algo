
**题目链接**：[https://leetcode.cn/problems/implement-queue-using-stacks/description](https://leetcode.cn/problems/implement-queue-using-stacks/description)

---

## 题目描述

请你仅使用两个栈实现先入先出队列。队列应当支持一般队列支持的所有操作（`push`、`pop`、`peek`、`empty`）：

实现 `MyQueue` 类：

- `void push(int x)` 将元素 x 推到队列的末尾
- `int pop()` 从队列的开头移除并返回元素
- `int peek()` 返回队列开头的元素
- `boolean empty()` 如果队列为空，返回 `true` ；否则，返回 `false`

**说明：**

- 你 **只能** 使用标准的栈操作 —— 也就是只有 `push to top`, `peek/pop from top`, `size`, 和 `is empty` 操作是合法的。
- 你所使用的语言也许不支持栈。你可以使用 list 或者 deque（双端队列）来模拟一个栈，只要是标准的栈操作即可。

**示例 1：**

**输入：**
`["MyQueue", "push", "push", "peek", "pop", "empty"]`
`["MyQueue", "push", "push", "peek", "pop", "empty"]`
**输出：**
`[null, null, null, 1, 1, false]`

**解释：**
MyQueue myQueue = new MyQueue();
myQueue.push(1); // queue is: [1]
myQueue.push(2); // queue is: [1, 2] (leftmost is front of the queue)
myQueue.peek(); // return 1
myQueue.pop(); // return 1, queue is [2]
myQueue.empty(); // return false

**提示：**

- `1 <= x <= 9`
- 最多调用 `100` 次 `push`、`pop`、`peek` 和 `empty`
- 假设所有操作都是有效的 （例如，一个空的队列不会调用 `pop` 或者 `peek` 操作）

**进阶：**

- 你能否实现每个操作均摊时间复杂度为 `O(1)` 的队列？换句话说，执行 `n` 个操作的总时间复杂度为 `O(n)` ，即使其中一个操作可能花费较长时间。

## 解题思路 ：

和[[155. 最小栈]]类似，引入一个辅助栈。


## 代码实现

## PHP 解法

```php
class MyQueue {
    /**
     */
    private $stack;
    function __construct() {
        $this->stack = [];
    }
  
    /**
     * @param Integer $x
     * @return NULL
     */
    function push($x) {
        array_push($this->stack,$x);
    }
  
    /**
     * @return Integer
     */
    function pop() {
        return array_shift($this->stack);
    }
  
    /**
     * @return Integer
     */
    function peek() {
        return current($this->stack);
    }
  
    /**
     * @return Boolean
     */
    function empty() {
        if(empty($this->stack)){
            return true;
        }else{
            return false;
        }
    }
}

/**
 * Your MyQueue object will be instantiated and called as such:
 * $obj = MyQueue();
 * $obj->push($x);
 * $ret_2 = $obj->pop();
 * $ret_3 = $obj->peek();
 * $ret_4 = $obj->empty();
 */
```

## Go 解法

```go
type MyQueue struct {
    stack []int
    reverStack []int
}


func Constructor() MyQueue {
    return MyQueue{
        stack:[]int{},
        reverStack:[]int{},
    }
}


func (this *MyQueue) Push(x int)  {
    this.stack = append(this.stack,x)
}


func (this *MyQueue) Pop() int {
    peek := this.Peek()
    this.reverStack = this.reverStack[:len(this.reverStack)-1]
    return peek
}


func (this *MyQueue) Peek() int {
    if len(this.reverStack) != 0 {
        return this.reverStack[len(this.reverStack)-1]
    }
    if len(this.stack) == 0 {
        return -1
    }
    for len(this.stack) > 0 {
        n := len(this.stack)
        val := this.stack[n-1]
        this.stack = this.stack[:n-1]
        this.reverStack = append(this.reverStack,val)
    }
    return this.reverStack[len(this.reverStack)-1]
}


func (this *MyQueue) Empty() bool {
    if len(this.stack) == 0 && len(this.reverStack) == 0 {
        return true
    }else{
        return false
    }
}


/**
 * Your MyQueue object will be instantiated and called as such:
 * obj := Constructor();
 * obj.Push(x);
 * param_2 := obj.Pop();
 * param_3 := obj.Peek();
 * param_4 := obj.Empty();
 */
```
## 关键点

引入一个辅助栈。
主要考察栈的操作，以及栈和队列的差异。
栈是 先进后出。
队列是 先进先出。

在用数组或者slice模拟栈时，栈顶其实是数组的末尾元素。

栈的标准操作只有 `push()`  元素入栈；`pop() `栈顶元素出栈；`peek()`获取栈顶元素；

所以，题目中用栈实现队列，获取队列开头的元素，由于栈只能访问末尾（栈顶），而队列需要访问开头（队首），因此需引入辅助栈。

要想实现通过栈来获取队列的头元素，需要构造一个原栈的倒序栈，则将原栈所有元素依次弹出并压入辅助栈。这样，主栈的底部元素（即最早入队的元素）就变成了辅助栈的栈顶，可通过`peek()`获取倒序栈的栈顶元素（尾元素）来实现获取原栈的头元素。

## 复杂度分析

- 时间复杂度 O(N) ： push(), empty() 函数的时间复杂度为 O(1) ；peek() , pop() 函数在 N 次队首元素删除操作中总共需完成 N 个元素的倒序，均摊时间复杂度为 O(1) 。
- 空间复杂度 O(N) ： 最差情况下，栈 `A` 和 `B` 共保存 N 个元素。

#leetcode #栈 #队列 #设计
