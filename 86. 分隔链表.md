
**题目链接**：[https://leetcode.cn/problems/partition-list/description](https://leetcode.cn/problems/partition-list/description)

---

## 题目描述

给你一个链表的头节点 `head` 和一个特定值 `x` ，请你对链表进行分隔，使得所有 **小于** `x` 的节点都出现在 **大于或等于** `x` 的节点之前。

你应当 **保留** 两个分区中每个节点的初始相对位置。

**示例 1：**

![](https://assets.leetcode.com/uploads/2021/01/04/partition.jpg)

**输入：** head = [1,4,3,2,5,2], x = 3
**输出**：[1,2,2,4,3,5]

**示例 2：**

**输入：** head = [2,1], x = 2
**输出**：[1,2]

**提示：**

- 链表中节点的数目在范围 `[0, 200]` 内
- `-100 <= Node.val <= 100`
- `-200 <= x <= 200`

## 解题思路 ：双指针

构造2段空链表，然后拼接2段链表。

## 代码实现

## PHP 解法

```php
/**
 * Definition for a singly-linked list.
 * class ListNode {
 *     public $val = 0;
 *     public $next = null;
 *     function __construct($val = 0, $next = null) {
 *         $this->val = $val;
 *         $this->next = $next;
 *     }
 * }
 */
class Solution {

    /**
     * @param ListNode $head
     * @param Integer $x
     * @return ListNode
     */
    function partition($head, $x) {
        $left_dum = new ListNode();
        $right_dum = new ListNode();
        $left = $left_dum;
        $right = $right_dum;
        while($head != null){
            if($head->val < $x){
                $left->next = $head;
                $left = $left->next;
            }else{
                $right->next = $head;
                $right = $right->next;
            }
            $head = $head->next;
        }

        $left->next = $right_dum->next;
        $right->next = null;
        return $left_dum->next;
    }
}
```

## Go 解法

```go
/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */
func partition(head *ListNode, x int) *ListNode {
    left_dum := &ListNode{Val:0}
    right_dum := &ListNode{Val:0}

    left := left_dum
    right := right_dum

    for head != nil {
        if head.Val < x {
            left.Next = head
            left = left.Next
        }else{
            right.Next = head
            right = right.Next
        }
        head = head.Next
    }

    left.Next = right_dum.Next
    right.Next = nil
    
    return left_dum.Next
}
```
## 关键点

1. 链表循环时，要固定头节点，用一个新的变量去做循环，因为链表循环到最后只剩下最后一个节点了，获取不到全部的链表数据。

2. 右侧链表的最后节点要指向空，因为右侧节点最后一个节点，在原链表中有可能不是最后一个节点（如例题所示节点5），那这个节点会有一个next指向原节点（例题中最后一个节点2）。所以，如果不显示把这个节点的next指向null，则会形成环，导致代码死循环。

## 复杂度分析

- 时间复杂度 O(N) ： N是原链表长度，需要循环一次原链表。
- 空间复杂度 O(1) ： 假头节点使用常数大小的额外空间。

#leetcode #链表 #双指针
