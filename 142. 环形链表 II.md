
**题目链接**：[https://leetcode.cn/problems/linked-list-cycle-ii/description](https://leetcode.cn/problems/linked-list-cycle-ii/description)

---

## 题目描述

给定一个链表的头节点 `head`，返回链表开始入环的第一个节点。如果链表无环，则返回 `null`。

**示例 1：**

```
输入：head = [3,2,0,-4], pos = 1
输出：返回索引为 1 的链表节点
解释：链表中有一个环，其尾部连接到第二个节点。
```

**示例 2：**

```
输入：head = [1,2], pos = 0
输出：返回索引为 0 的链表节点
解释：链表中有一个环，其尾部连接到第一个节点。
```

**示例 3：**

```
输入：head = [1], pos = -1
输出：返回 null
解释：链表中没有环。
```

**提示：**

- 链表中节点的数目范围在 `[0, 10^4]` 内
- `-10^5 <= Node.val <= 10^5`
- `pos` 的值为 `-1` 或者链表中的一个有效索引

**进阶：** 你是否可以使用 `O(1)` 空间解决此题？

## 解题思路

### 步骤1：判断是否有环（快慢指针）

- 慢指针每次走 1 步，快指针每次走 2 步
- 如果快指针追上慢指针，说明有环
- 如果快指针走到 null，说明无环

### 步骤2：找环的入口（Floyd 判圈算法）

假设：
- 链表头到环入口的距离：`a`
- 环入口到相遇点的距离：`b`
- 相遇点回到环入口的距离：`c`

```
head → ... → 入口 → ... → 相遇点 → ... → 入口
      ←——a——→
                ←——b——→
                              ←——c——→
```

相遇时：
- 慢指针走了：`a + b`
- 快指针走了：`a + b + n(b + c)`（n 是快指针绕的圈数）

因为快指针速度是慢指针的 2 倍：
```
2(a + b) = a + b + n(b + c)
a + b = n(b + c)
a = (n-1)(b + c) + c
```

**关键结论：**
- 从 head 走 `a` 步到达环入口
- 从相遇点走 `a` 步（即 `(n-1)` 圈 + `c` 步）也到达环入口

所以步骤2中：
- 一个指针从 head 出发
- 一个指针从相遇点出发
- 两个指针每次都走 1 步
- 相遇点就是环入口

**注意：** slow 和 fast 相遇时指向同一个节点，所以步骤2用 slow 或 fast 作为起点效果完全相同。

**时间复杂度：** O(n)

**空间复杂度：** O(1)

## Go 解法

```go
/**
 * Definition for singly-linked list.
 * type ListNode struct {
 *     Val int
 *     Next *ListNode
 * }
 */
func detectCycle(head *ListNode) *ListNode {
    // 步骤1：判断是否有环，找相遇点
    slow, fast := head, head
    for fast != nil && fast.Next != nil {
        slow = slow.Next
        fast = fast.Next.Next
        if slow == fast {
            break  // 有环，找到相遇点
        }
    }

    // 无环
    if fast == nil || fast.Next == nil {
        return nil
    }

    // 步骤2：找环入口
    p1 := head      // 从 head 出发
    p2 := slow      // 从相遇点出发（slow 和 fast 相遇时指向同一个节点）
    for p1 != p2 {
        p1 = p1.Next
        p2 = p2.Next
    }
    return p1
}
```

**说明：** slow 和 fast 相遇时指向同一个节点，所以步骤2用 slow 或 fast 效果相同。返回 p1 或 p2 都行，它们最终相等。

## PHP 解法

```php
/**
 * Definition for a singly-linked list.
 * class ListNode {
 *     public $val = 0;
 *     public $next = null;
 *     public function __construct($val = 0, $next = null) {
 *         $this->val = $val;
 *         $this->next = $next;
 *     }
 * }
 */
class Solution {

    /**
     * @param ListNode $head
     * @return ListNode
     */
    function detectCycle($head) {
        // 步骤1：判断是否有环，找相遇点
        $slow = $head;
        $fast = $head;
        while ($fast != null && $fast->next != null) {
            $slow = $slow->next;
            $fast = $fast->next->next;
            if ($slow === $fast) {
                break;  // 有环，找到相遇点
            }
        }

        // 无环
        if ($fast == null || $fast->next == null) {
            return null;
        }

        // 步骤2：找环入口
        $p1 = $head;      // 从 head 出发
        $p2 = $slow;      // 从相遇点出发
        while ($p1 !== $p2) {
            $p1 = $p1->next;
            $p2 = $p2->next;
        }
        return $p1;
    }
}
```

**说明：** PHP 中比较对象引用用 `===`。slow 和 fast 相遇时指向同一个节点，步骤2用 slow 或 fast 起点效果相同。

#leetcode #链表 #双指针 #快慢指针
