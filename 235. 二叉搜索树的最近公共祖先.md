
**题目链接**：[https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-search-tree/description](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-search-tree/description)

---
## 题目描述

给定一个二叉搜索树，找到该树中两个指定节点的最近公共祖先。

百度百科中最近公共祖先的定义为："对于有根树 T 的两个结点 p、q，最近公共祖先表示为一个结点 x，满足 x 是 p、q 的祖先且 x 的深度尽可能大（一个节点也可以是它自己的祖先）。"

例如，给定如下二叉搜索树：`root = [6,2,8,0,4,7,9,null,null,3,5]`

```
        ⑥
       /  \
      ②    ⑧
     / \   / \
    ⓪  ④ ⑦  ⑨
       / \
      ③  ⑤
```

**示例 1：**

```
输入：root = [6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 8
输出：6
解释：节点 2 和节点 8 的最近公共祖先是 6。
```

**示例 2：**

```
输入：root = [6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 4
输出：2
解释：节点 2 和节点 4 的最近公共祖先是 2，因为根据定义最近公共祖先节点可以为节点本身。
```

**说明：**

- 所有节点的值都是唯一的。
- `p`、`q` 为不同节点且均存在于给定的二叉搜索树中。

---

## 解题思路

### 核心思想：利用 BST 的大小关系

先回顾一下 BST 的核心性质（上一题刚讲过）：

> **左子树所有节点 < 根节点 < 右子树所有节点**

利用这个性质，判断 p 和 q 的位置关系，只有三种情况：

```
情况一：p 和 q 都在左子树
        root
        /   \
      p,q  ...
      ↓
    LCA 在左子树

情况二：p 和 q 都在右子树
        root
        /   \
      ...  p,q
            ↓
      LCA 在右子树

情况三：p 和 q 在异侧（或其一就是 root）
        root  ← LCA！
        /   \
       p     q
       ↓
    root 就是 LCA
```

**判断依据（节点值决定方向）：**

```php
// 当前节点比 p 和 q 都大 → p 和 q 都在左子树
if ($cur->val > $p->val && $cur->val > $q->val) → 往左走

// 当前节点比 p 和 q 都小 → p 和 q 都在右子树
if ($cur->val < $p->val && $cur->val < $q->val) → 往右走

// 否则（一大一小 或 等于其中一个）→ 当前节点就是 LCA
else → 返回当前节点
```

**与 [[LeetCode/236. 二叉树的最近公共祖先]] 的区别：**

| | 普通二叉树 LCA（236） | 二叉搜索树 LCA（235，本题） |
|--|----------------------|--------------------------|
| 解法 | 后序遍历递归，O(n) | **根据值判断方向**，O(h) |
| 所需信息 | 不知道左右大小关系 | 利用左 < 根 < 右 |
| 效率 | 需要遍历整个树 | 从根到 LCA 一条路径即可 |

### 算法流程

1. 从根节点开始
2. 如果 `root.val > p.val && root.val > q.val` → p、q 都在左子树，往左走
3. 如果 `root.val < p.val && root.val < q.val` → p、q 都在右子树，往右走
4. 否则 → 当前节点就是 LCA，返回

### 复杂度分析

- **时间复杂度**：O(h) — h 为树高，只需从根走到 LCA 所在的那一层
  - 平均 O(log n)，最坏（链状树）O(n)
- **空间复杂度**：
  - 递归法：O(h) — 递归栈
  - 迭代法：O(1) — 只需要一个指针

## PHP 解法

### 解法一：递归

```php
class Solution {

    /**
     * @param TreeNode $root
     * @param TreeNode $p
     * @param TreeNode $q
     * @return TreeNode
     */
    function lowestCommonAncestor($root, $p, $q) {
        // p 和 q 都在左子树
        if ($root->val > $p->val && $root->val > $q->val) {
            return $this->lowestCommonAncestor($root->left, $p, $q);
        }

        // p 和 q 都在右子树
        if ($root->val < $p->val && $root->val < $q->val) {
            return $this->lowestCommonAncestor($root->right, $p, $q);
        }

        // 否则当前节点就是 LCA
        return $root;
    }
}
```

**说明：**
- 对比 236 题，这里不需要递归完整个树——每次递归只往一边走
- 也不需要 `$left` 和 `$right` 两路递归后判断，因为 BST 直接告诉了你方向
- 实际上这一路递归就是**沿着从根到 LCA 的一条路径走下去**，找到就返回

### 解法二：迭代（推荐——更简洁）

```php
class Solution {

    /**
     * @param TreeNode $root
     * @param TreeNode $p
     * @param TreeNode $q
     * @return TreeNode
     */
    function lowestCommonAncestor($root, $p, $q) {
        $cur = $root;

        while ($cur !== null) {
            // p 和 q 都在左子树
            if ($cur->val > $p->val && $cur->val > $q->val) {
                $cur = $cur->left;
            // p 和 q 都在右子树
            } elseif ($cur->val < $p->val && $cur->val < $q->val) {
                $cur = $cur->right;
            // 否则当前节点就是 LCA
            } else {
                return $cur;
            }
        }

        return null;
    }
}
```

**说明：**
- 迭代法比递归更优——空间 O(1)，没有递归栈
- 逻辑完全一样，只是把递归换成了 while 循环

## Go 解法

### 解法一：递归

```go
/**
 * Definition for a binary tree node.
 * type TreeNode struct {
 *     Val   int
 *     Left  *TreeNode
 *     Right *TreeNode
 * }
 */

func lowestCommonAncestor(root, p, q *TreeNode) *TreeNode {
    // p 和 q 都在左子树
    if root.Val > p.Val && root.Val > q.Val {
        return lowestCommonAncestor(root.Left, p, q)
    }

    // p 和 q 都在右子树
    if root.Val < p.Val && root.Val < q.Val {
        return lowestCommonAncestor(root.Right, p, q)
    }

    // 否则当前节点就是 LCA
    return root
}
```

### 解法二：迭代（推荐）

```go
func lowestCommonAncestor(root, p, q *TreeNode) *TreeNode {
    cur := root

    for cur != nil {
        switch {
        case cur.Val > p.Val && cur.Val > q.Val:
            cur = cur.Left
        case cur.Val < p.Val && cur.Val < q.Val:
            cur = cur.Right
        default:
            return cur
        }
    }

    return nil
}
```

**说明：**
- 用了 Go 的 `switch` 多条件判断语法，比多个 `if-else` 更清晰
- 迭代法空间 O(1)，是这道题的最优解

## Go 与 PHP 差异总结

| 对比项 | PHP | Go |
|--------|-----|-----|
| **递归写法** | `$this->lowestCommonAncestor(...)` | `lowestCommonAncestor(...)` |
| **属性访问** | `$root->val` | `root.Val` |
| **迭代循环** | `while ($cur !== null)` | `for cur != nil` |
| **多条件判断** | `if-elseif-else` | `if-else` 或 `switch` |
| **null/nil** | `null` | `nil` |
| **返回值类型** | 弱类型，`@return TreeNode` | 强类型 `*TreeNode` |
| **属性命名** | 小驼峰：`$cur->left`, `$cur->right` | 大驼峰：`cur.Left`, `cur.Right`（导出字段） |

### 重要差异：这题为什么比 236 简单？

236（普通二叉树）：
```php
// 不知道左右大小关系，只能两边都递归
$left  = $this->lowestCommonAncestor($root->left, $p, $q);
$right = $this->lowestCommonAncestor($root->right, $p, $q);
// 然后根据结果判断
```

235（BST，本题）：
```php
// 根据值的大小直接知道往哪边走
if ($root->val > $p->val && $root->val > $q->val) {
    return $this->lowestCommonAncestor($root->left, $p, $q); // 只走一边！
}
```

## 扩展知识

### 三种 LCA 问题的递进关系

| 题目 | 数据结构 | 解法 | 时间复杂度 |
|------|---------|------|-----------|
| [[LeetCode/235. 二叉搜索树的最近公共祖先]]（本题） | 二叉搜索树 | 根据值比较单向走 | O(h) |
| [[LeetCode/236. 二叉树的最近公共祖先]] | 普通二叉树 | 后序遍历递归 | O(n) |
| 非树结构（如多叉树 + 父指针） | 任意树 | 哈希表记录父节点路径 | O(n) |

BST 的 LCA 是最简单、最高效的版本，因为它有"方向感"。

### 一个思考：如果 p 和 q 不在树中怎么办？

题目保证 p、q 都在树中，但如果你要实现一个通用的 LCA，BST 版本同样很好扩展：
- 沿着路径走，如果某一步应该往左但左子节点不存在（或往右但没有右子节点），则说明 p 或 q 不在树中
- 236 的递归版本也可以扩展到找不到的情况（`$left` 和 `$right` 都为空时返回 null）

#leetcode #二叉树 #BST #LCA #递归 #迭代
