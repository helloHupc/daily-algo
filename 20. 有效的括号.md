
**题目链接**：[https://leetcode.cn/problems/valid-parentheses/description](https://leetcode.cn/problems/valid-parentheses/description)

---

## 题目描述

给定一个只包括 `'('`，`')'`，`'{'`，`'}'`，`'['`，`']'` 的字符串 `s` ，判断字符串是否有效。

有效字符串需满足：

1. 左括号必须用相同类型的右括号闭合。
2. 左括号必须以正确的顺序闭合。
3. 每个右括号都有一个对应的相同类型的左括号。

**示例 1：**

**输入：** s = "()"

**输出：** true

**示例 2：**

**输入：** s = "()[]{}"

**输出：** true

**示例 3：**

**输入：** s = "(]"

**输出：** false

**示例 4：**

**输入：** s = "([])"

**输出：** true

**示例 5：**

**输入：** s = "([)]"

**输出：** false

**提示：**

- `1 <= s.length <= 104`
- `s` 仅由括号 `'()[]{}'` 组成

## 解题思路 ：

用栈来处理。


## 代码实现

## PHP 解法

```php
class Solution {

    /**
     * @param String $s
     * @return Boolean
     */
    function isValid($s) {
        $map = [
            '('=>')',
            '['=>']',
            '{'=>'}',
            '?'=>'?'
        ];
        $stack = ['?'];

        $arr = str_split($s);

        for($i=0;$i<count($arr);$i++){
            if(array_key_exists($arr[$i],$map)){
                array_push($stack,$arr[$i]);
            }else{
                $l = array_pop($stack);
                if($map[$l] != $arr[$i]){
                    return false;
                }
            }
        }

        return count($stack) == 1;
        
    }
}
```

## Go 解法

```go
func isValid(s string) bool {
    dict := map[rune]rune{
        '(':')',
        '[':']',
        '{':'}',
        '?':'?',
    }

    stack := []rune{'?'}

    for _,c := range s {
        // 左括号入栈 右括号用出栈数据匹配
        if  _,ok := dict[c]; ok {
            stack = append(stack,c)
        }else{
            top := stack[len(stack)-1]
            stack = stack[:len(stack)-1]
            if dict[top] != c {
                return false
            } 
        }
    }

    return len(stack) == 1   
}
```
## 关键点

重点在于理解如何使用栈，怎样用栈来组织数据。

构建一个map字典来映射左右括号，方便判断是否配对。

解决边界问题。

栈中预设值，解决栈为空时，pop操作报错问题。

## 复杂度分析

- 时间复杂度 O(N) ： 遍历字符串。
- 空间复杂度 O(N) ： 构造的map字典和栈变量的值随字符串的数量线性增加。

#leetcode #栈 #字符串
